# Deterministic Skeleton Rendering Verification Evidence

**Execution date:** 2026-08-06 (America/Toronto)

**Status:** P1 Task 6 implementation, evidence-backed repairs, independent re-review, and settled verification are complete. Gate 3 verified-final-diff approval remains pending.

**Approved comparison base:** `5ed163040191d1a67052afdc08a3a9c9a379e6ab`

**Verified source candidate:** `d5f01353d5ac91b908889d41e5bf5a8c1c7e3f85`

**Verified source comparison:** `5ed163040191d1a67052afdc08a3a9c9a379e6ab..d5f01353d5ac91b908889d41e5bf5a8c1c7e3f85`

**Commits:**

- `ed33e97` — `Plan deterministic skeleton rendering`
- `bd9e176` — `Render deterministic skeletons`
- `742bb4d` — `Align skeletons with architecture contracts`
- `1ab75e5` — `Harden skeleton rendering verification`
- `d5f0135` — `Protect generation read boundaries`

The approved preparation artifacts were committed on local `main`. Implementation and review used isolated worktree `/private/tmp/egeria-scaffold-p1-task-6` on branch `p1-task-6-skeleton-rendering`. Remote refs were not fetched because the approved local P1 sequence and current official upstream documentation, rather than remote integration, own this increment.

At settled verification, the primary checkout remained on local `main`, thirty-nine commits ahead of unrefreshed local `origin/main`. Its user-owned root `AGENTS.md` edit and two untracked semantic-naming preparation/plan files remained untouched and excluded. A separate `semantic-naming-enforcement` worktree also remained outside this comparison.

## Implemented boundary

Task 6 adds a private `renderSkeleton` boundary that:

- composes the existing exact six-capability catalog, profile recipes, resolver, project contract, canonical JSON, and ownership materializer;
- reads only 20 explicit common templates plus the exact profile overlay;
- accepts only `projectName`, `displayNameJson`, and `workerName` textual tokens;
- validates source and destination paths, rejects duplicate destinations, normalizes LF bytes, and returns files in direct code-point order;
- snapshots caller-owned package-version strings before validation or asynchronous I/O and structurally inserts only the validated exact versions;
- externalizes runtime copy in strict YAML 1.2 files parsed into exact typed shapes;
- returns the desired project, resolved recipe, generated bytes, and validated hybrid ownership descriptors entirely in memory; and
- keeps generated presentation pure and Cloudflare imports/types confined to approved generated configuration boundaries.

`portfolio` materializes one public page, 21 files, five capabilities, and 40 ownership descriptors. `site` materializes the common files plus `/about`, for 23 files, six capabilities, and 42 descriptors. Both are lightweight workspaces with only `apps/web`; no `apps/jobs` or generated local package exists.

Task 6 performs no destination write, `.egeria` state update, migration, dependency install, generated lockfile, generated build, CLI action, publication, deployment, or provider mutation.

## Exact changed source files

The frozen source comparison contains these 41 paths:

```text
docs/architecture/capability-model.md
docs/architecture/enforcement-map.md
docs/architecture/package-ownership.md
docs/implementation-evidence/2026-08-06-deterministic-skeleton-rendering-preparation.md
docs/superpowers/plans/2026-08-05-p1-builder-kernel.md
docs/superpowers/plans/2026-08-06-deterministic-skeleton-rendering.md
packages/builder-core/AGENTS.md
packages/builder-core/README.md
packages/builder-core/src/catalog/capability-catalog.ts
packages/builder-core/src/generation/render-skeleton.ts
packages/builder-core/src/generation/render-template.ts
packages/builder-core/src/generation/template-catalog.ts
packages/builder-core/src/index.ts
packages/builder-core/templates/common/.gitignore.template
packages/builder-core/templates/common/.nvmrc
packages/builder-core/templates/common/AGENTS.md.template
packages/builder-core/templates/common/README.md.template
packages/builder-core/templates/common/apps/web/AGENTS.md.template
packages/builder-core/templates/common/apps/web/app/globals.css
packages/builder-core/templates/common/apps/web/app/layout.tsx
packages/builder-core/templates/common/apps/web/app/page.tsx
packages/builder-core/templates/common/apps/web/eslint.config.mjs
packages/builder-core/templates/common/apps/web/next.config.ts
packages/builder-core/templates/common/apps/web/open-next.config.ts
packages/builder-core/templates/common/apps/web/package.json.template
packages/builder-core/templates/common/apps/web/src/content/content-schema.ts
packages/builder-core/templates/common/apps/web/src/content/read-content.ts
packages/builder-core/templates/common/apps/web/src/infrastructure/observability/installed-capability.ts
packages/builder-core/templates/common/apps/web/src/presentation/content-page.tsx
packages/builder-core/templates/common/apps/web/tsconfig.json
packages/builder-core/templates/common/apps/web/wrangler.jsonc.template
packages/builder-core/templates/common/package.json.template
packages/builder-core/templates/common/pnpm-workspace.yaml
packages/builder-core/templates/portfolio/apps/web/content/en-CA/site.yaml.template
packages/builder-core/templates/site/apps/web/app/about/page.tsx
packages/builder-core/templates/site/apps/web/content/en-CA/about.yaml.template
packages/builder-core/templates/site/apps/web/content/en-CA/site.yaml.template
packages/builder-core/tests/diagnostics.test.mjs
packages/builder-core/tests/render-skeleton.test.mjs
packages/builder-core/tests/resolution.test.mjs
tests/package-boundaries/private-packages.test.mjs
```

The final gate-artifact commit additionally contains this record and `docs/review-packets/2026-08-06-p1-task-6-deterministic-skeleton-rendering.md`.

## TDD and repair record

### Initial renderer RED and GREEN

The focused renderer test first failed because `dist/generation/render-template.js` did not exist. After the minimum pure token/path/newline implementation, its four focused mechanics tests passed.

The public rendering tests then ran with the four mechanics tests green and failed because `renderSkeleton` was not exported. After the explicit catalog, templates, composition, ownership, and root export were implemented, the focused renderer suite passed 11/11. The package-boundary test separately failed on the deliberately stale Task 5 source/template allowlists and direct-owner text, then passed 22/22 after the atomic Task 6 boundary update.

### YAML architecture correction

Requirements review found that the initial JSON content design in the narrow Task 6 plans and first implementation contradicted accepted ADR-0008 and the approved source roadmap, both of which require YAML 1.2 structured content. Accepted ADRs control this decision, so no user choice was needed under the approved amendment authority.

Repair tests failed causally because the renderer still emitted `.json`, lacked the generated `yaml` dependency and capability-owned package pointer, returned 39/41 rather than 40/42 descriptors, and did not match the amended exact catalog/template allowlists. The minimum correction changed the catalog, probes, ownership, templates, fixed-path readers, strict parser, tests, and direct plans together. A diagnostic fixture then exposed its own stale manifest by failing ownership materialization; adding the exact `yaml: 2.9.0` fixture dependency made its 22/22 focused diagnostic tests pass.

### Security RED and GREEN

The input-security reviewer reproduced a time-of-check/time-of-use defect. `request.packageVersions` was validated, asynchronous template reads yielded, and manifest enrichment then re-read the caller-owned object. A caller mutation inserted `file:../../attacker` and `https://attacker.invalid/package.tgz` while resolved probes retained the validated recipe.

The automated regression test observed the intended RED:

```text
Expected: 0.1.0
Actual:   file:../../attacker
exit 1; focused mutation test 0/1
```

`renderSkeleton` now copies both strings before catalog validation and the first `await`, and reuses only that snapshot. The same focused test passed 1/1; the specialist's original reproduction then retained `0.1.0` in both manifest and probes and contained neither injected value.

### Test-evidence repairs

The test-evidence reviewer identified four protection gaps without a current behavior failure. Accepted repairs now:

- transpile and execute the emitted content module, accept both generated YAML documents, and reject duplicate keys, unknown tags, aliases, multiple documents, extra keys, empty headings, and duplicate links with `CONTENT_INVALID`;
- render both profiles into the in-memory repository reader and prove every resolved capability infers `probable` with every probe `present`;
- broaden generated-source checks for Cloudflare imports and ambient binding types and constrain presentation to its sole type-only content import with no filesystem, environment, fetch, or state-hook behavior; and
- preserve template bytes across rendering while applying the source-level write-capability prohibition across the exact three generation modules, permitting exactly one `node:fs/promises` import and exactly `{ readFile }`.

The renderer suite passed 16/16 after these additions. Final bounded re-review closed every gap and explicitly accepted the source-level no-write enforcement within the exact three-module allowlist.

## Deterministic output evidence

Fixed request values were `projectName: "acme-studio"`, `displayName: "Acme Studio"`, and synthetic in-memory standards/observability versions `0.1.0`. The SHA-256 input is each sorted path, NUL, exact file bytes, NUL.

| Profile | Files | Surfaces | Resolved capabilities | SHA-256 |
| --- | ---: | ---: | --- | --- |
| `portfolio` | 21 | 40 | `standards`, `content-files`, `section-composition`, `deployment-cloudflare`, `observability` | `f1ed2e323a28f4ea4ae27bfbb13f98a3dd9e1a887c9458728c225912ac054825` |
| `site` | 23 | 42 | portfolio set plus `site-routing` | `97c52116409cf432e52adcb7175709a5002d33f8aa53fc64970d2cb5e0c87e91` |

Repeated same-input renders were byte-identical. Generated-file-to-inference composition found every declared probe present for both profiles. Missing `.egeria/state.json` is expected because installed state is Task 7.

## Settled commands and results

All repository commands used Node.js `v22.23.0`, pnpm `11.20.0`, `CI=true`, the pinned pnpm executable, and `rtk`.

| Command | Result |
| --- | --- |
| `rtk env CI=true /Users/CoveMB/.volta/tools/image/pnpm/11.20.0/bin/pnpm --filter builder-core run verify` | exit `0`; build, checked schemas, 85/85 tests, no-emit typecheck, and zero-warning lint passed |
| `rtk env CI=true /Users/CoveMB/.volta/tools/image/pnpm/11.20.0/bin/pnpm run test:package-boundaries` | exit `0`; 22/22 passed |
| `rtk env CI=true /Users/CoveMB/.volta/tools/image/pnpm/11.20.0/bin/pnpm run test:constitution` | exit `0`; 13/13 passed, including repository-local Markdown links |
| renderer snapshot command using `node:crypto.createHash("sha256")` | exit `0`; counts and hashes match the table above |
| `rtk git diff --check 5ed1630..d5f0135` | exit `0` |
| `rtk sha256sum` on the approved source plan and `pnpm-lock.yaml` | unchanged hashes `30860d49…5b05` and `f4542842…fc0` |
| `rtk git status --short --branch` | clean isolated implementation branch before gate-artifact creation |

Two local command failures did not exercise product behavior:

- the desktop fallback pnpm was `11.16.0` under Node `24.14.0` and correctly stopped on the repository engine pins; using the required Volta pnpm path ran Node `22.23.0` and pnpm `11.20.0`;
- a reviewer's first build omitted `CI=true` and stopped before build with `ERR_PNPM_META_FETCH_FAIL` and `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`; the canonical pinned command passed.

The unchanged P0.2 compatibility proof and dependency audit were not rerun after source-only repairs. Task 6 did not change the builder manifest, lockfile, proof, workflow, or installed dependency graph.

## Official documentation and advisory evidence

Preparation revalidated current sources on 2026-08-06. Material conclusions retained here are:

- current Next.js installation and TypeScript guidance support the generated App Router/root-layout, direct ESLint, `next typegen`, and ignored generated declaration boundaries;
- current OpenNext and Cloudflare guidance supports Next.js 16, `nodejs_compat`, `.open-next/worker.js`, `.open-next/assets`, and the retained accepted compatibility date;
- exact selected Next `16.3.0`, React/React DOM `19.2.8`, OpenNext `1.20.2`, Wrangler `4.118.0`, YAML `2.9.0`, and tooling packages existed, were not deprecated, and had compatible peer requirements;
- official `yaml` documentation supports the strict YAML 1.2 parser options exercised by the emitted-parser tests;
- the GitHub Advisory Database returned no package-specific `yaml` advisory, and the approved `pnpm audit --prod` returned `No known vulnerabilities found` for the unchanged exact lock graph;
- selected Next, React, OpenNext, Wrangler, and pnpm versions were outside the reviewed affected advisory ranges; and
- official Node.js `22.23.2` fixes HIGH-severity issues while this repository remains pinned to accepted compatibility version `22.23.0`.

Source URLs, registry observations, advisory links, exact peer constraints, and the retrieval boundary are recorded in the [preparation evidence](2026-08-06-deterministic-skeleton-rendering-preparation.md).

## Independent review dispositions

Four independent read-only lanes reviewed the frozen source, and bounded follow-ups reviewed only their repairs.

- **Requirements:** accepted one high plan/implementation conflict: JSON structured content contradicted accepted ADR-0008 and the source roadmap's YAML 1.2 contract. Commit `742bb4d` aligned every direct consumer; re-review found no remaining blocker and no migration need because no generated repository or state exists.
- **Architecture and anti-overengineering:** accepted one medium evidence defect: the enforcement map called generated Cloudflare isolation actual without a rendered-source assertion. Commit `742bb4d` added that bounded gate; re-review found it closed and found no related architecture or overengineering regression.
- **Template/input security:** accepted one high package-source validation race. Commit `1ab75e5` added the causal regression and immutable validated snapshot. Re-review reproduced the repaired behavior and found no remaining security blocker or introduced regression.
- **Test evidence:** accepted four medium protection gaps covering emitted parser behavior, output/inference composition, no-write breadth, and Cloudflare/presentation purity. Commits `1ab75e5` and `d5f0135` added the bounded checks. Final re-review closed all four with no remaining material blocker.

Reviewers performed no edits, recursive delegation, GitHub response, workflow dispatch, deployment, provider action, or external mutation.

## Evidence limits and residual risks

- Task 6 does not write or install a generated repository. Therefore no generated lockfile, frozen install, Next build, OpenNext build, workerd execution, preview, or deployment was exercised. Task 7 owns those gates.
- The emitted YAML parser is executed after in-memory TypeScript transpilation against the builder workspace's exact `yaml` package. This proves its parser/shape behavior, not its integration into a standalone generated Next/OpenNext bundle.
- Generated readers use fixed `new URL(..., import.meta.url)` paths. File tracing and runtime inclusion remain assumptions until the separately gated generated build.
- No-write evidence combines exact template snapshots with a source-level prohibition across the exact three generation modules. It is not system-call instrumentation and does not make hostile-kernel or compromised-runtime claims.
- Synthetic `0.1.0` standards and observability versions are contract data only. Those public artifacts are not yet verified as published and installable; their separate publication prerequisite still blocks Task 7.
- Node `22.23.0` is the accepted compatibility pin but is not security-current. A separate runtime-pin compatibility increment is required before P1 closure or any current-security/release claim.
- Automated tests do not prove visual quality, translation quality, human usability, accessibility conformance, deployment safety, production safety, or a general security audit. No WCAG conformance claim is made.

## Rollback and recovery

- Revert source commits in reverse order: `d5f0135`, `1ab75e5`, `742bb4d`, then `bd9e176`. Use focused revert commits; do not reset shared history.
- Revert `ed33e97` separately only if the preparation evidence and approved Task 6 plan must be withdrawn from `main`.
- Rebuild builder-core after source rollback. Ignored `dist` output is reproducible and non-authoritative.
- Revert the final gate-artifact commit separately if this evidence and its review packet must be withdrawn.
- No builder dependency or lockfile rollback exists.
- No `.egeria` state, migration record, generated repository, persistent data, deployment, package publication, provider resource, or external system requires recovery.

## Gate disposition

Task 6 meets its implementation, review, and deterministic source checkpoint. The source candidate is `d5f01353d5ac91b908889d41e5bf5a8c1c7e3f85`. The final handoff also includes this evidence and the Gate 3 packet in one focused commit. Task 7, public-package publication, the Node pin increment, and every external action remain outside this approval request.
