# Site Routing Certification Receipt

**Execution date:** `2026-08-26 America/Toronto (EDT, UTC-04:00)`

**Certification receipt status:** `complete`

**Certification reviewer decision:** `accepted`

**Certification unresolved prompts:** `none`

**Certification capability:** `site-routing`

**Certification descriptor version:** `0.3.0`

**Certification behavior-contract digest:** `sha256:d716a1c93f8f40db33e54612c85d521fbd6ba13cd142d35ab0c39fa9c4b9647e`

**Certification evidence revision:** `78d2b0d0e92d68b47d5b4344e7ebdca8e059661e`

**Passed certification outcomes:** `fresh-scaffold`

**Reviewed certification outcomes:** `fresh-scaffold`

This content-safe receipt records the causal local certification and approved registry transition for the exact subject.

## Direct predecessor and authority

- Direct predecessor: exact `section-composition@0.3.0` certification delivered through merged pull request 57.
- Accepted predecessor revision: `main@8869a0c479fa33e2da5c43758b9f068a2309c01e`.
- Predecessor acceptance check: successful post-merge Repository quality run `32939995555` at the accepted revision.
- Causal predecessor evidence: section-composition revision `f74459c8833833186bb651c116ed524e51044677`.
- Human authority: the user approved the exact subject plan, bounded local journey, evidence acceptance, registry transition, final reviews, CodeRabbit corrections, and pull-request delivery in advance.

## Evidence command and environment

The exact clean revision ran:

```text
pnpm run verify:site-routing-certification -- --revision 78d2b0d0e92d68b47d5b4344e7ebdca8e059661e
```

The runner used repository-pinned Node.js `22.23.2` and pnpm `11.20.0`. Frozen dependency installation and Chromium installation used bounded network access inside identity-owned disposable roots. No credential, provider account, deployment, protected environment, persistent external state, or spend was required.

A preliminary invocation supplied a non-HEAD 40-character revision and was rejected with `CERTIFICATION_REVISION_MISMATCH` before any disposable journey began. An earlier candidate journey passed before independent review identified that the focused fixture parsed the route content without rendering the actual route module; that candidate was superseded. The first corrected run failed closed when sandbox DNS denial made its cold-store install time out, and a network-enabled rerun then failed closed because the TSX fixture was routed to the generated Vitest project that accepts only `.test.ts` files. Neither failed attempt is evidence. The final exact revision above routes the TSX render through a fixture-only jsdom configuration, mirrors the generated Next raw-content file classes within the generated content root, and passed the complete causal journey.

## Fresh-scaffold outcome

The compiled CLI created one disposable `site@0.10.0` project with exact `site-routing@0.3.0`. Fresh inference confirmed the subject, doctor returned healthy with no diagnostics, and exact diff returned equal with no differences.

The fixed generated-project verifier passed, in order:

1. pnpm version and frozen install;
2. peer-dependency, dependency-audit, and registry-signature checks;
3. lint, Cloudflare type generation, typecheck, unit tests, and component tests;
4. Next.js and OpenNext builds; and
5. Chromium installation plus development and workerd-preview browser journeys.

The disposable subject-specific overlay then passed a second frozen install, a focused Vitest component contract, Chromium installation, and a development-browser journey. The causal checks established:

- exact installed `site-routing@0.3.0` source-generated, repository-stateful, reviewed-removal state;
- exactly two application-owned ordinary-file surfaces, `apps/web/app/about/page.tsx` and `apps/web/content/en-CA/about.yaml`, with ordinary SHA-256 fingerprints;
- exact parsing of the generated About introduction and working-principles sections, followed by execution and rendering of the actual generated About route module from the generated content sources;
- a successful direct `/about` document response with the generated headings and body copy; and
- link-driven navigation from `/` to `/about` and back to `/`, with exact Home and About destinations.

## Revision, cleanup, and privacy guards

The runner required the exact 40-character evidence revision, empty staged, tracked, and non-ignored-untracked status, and ordinary tracked Git index entries before and after the journey. It refused revision drift, worktree drift, hidden index flags, malformed arguments, invalid journey output, missing or reordered verifier checks, fixture failure, and cleanup failure.

Only identity-checked local temporary owners were removed. Repository sources remained unchanged across the evidence run. No secret, environment value, private URL, machine-specific path, generated source dump, child-process output, registry response, browser profile, screenshot, or raw log is retained here.

## Claim boundary

This receipt supports only the exact local `fresh-scaffold` outcome for `site-routing@0.3.0`, behavior-contract digest `sha256:d716a1c93f8f40db33e54612c85d521fbd6ba13cd142d35ab0c39fa9c4b9647e`, site recipe `0.10.0`, locale `en-CA`, the evidence revision, and the recorded checks.

It does not establish route-aware metadata, production-complete navigation semantics, redirects, sitemap or robots output, nested-route behavior, visual or design quality, performance, deployment, provider behavior, publication, broad security certification, privacy completeness, production readiness, translation quality, human accessibility, assistive-technology usability, or WCAG conformance. It does not certify another recipe or version, an existing-repository lifecycle, removal, migration, recovery, P3 closure, or P3B readiness.

## Acceptance record

- Exact subject, digest, revision, profile, recipe, locale, and evidence kind accepted: `yes`
- Compiled creation, state/inference agreement, diagnostics, and exact diff accepted: `yes`
- Fixed generated-project verifier results accepted: `yes`
- Exact ownership, state, content, direct-route, and navigation fixture results accepted: `yes`
- Cleanup, privacy exclusions, and claim boundary accepted: `yes`
- Registry transition approved: `yes`
- Review revision: `78d2b0d0e92d68b47d5b4344e7ebdca8e059661e`
- Rerun trigger: any material descriptor, required-evidence contract, runner, fixture, owned generated surface, route/content behavior, registry, verifier, or evidence defect requires a new clean evidence revision and complete affected rerun
