# Content Files Certification Receipt

**Execution date:** `2026-08-25 America/Toronto (EDT, UTC-04:00)`

**Certification receipt status:** `complete`

**Certification reviewer decision:** `accepted`

**Certification unresolved prompts:** `none`

**Certification capability:** `content-files`

**Certification descriptor version:** `0.4.0`

**Certification behavior-contract digest:** `sha256:5ae35debef622dc0fb9eeee3889e79a72fd6ff28eb730865bfe95e8674c9ff05`

**Certification evidence revision:** `f03b9f624c370728f678924ce34e5287558d2a87`

**Passed certification outcomes:** `fresh-scaffold`

**Reviewed certification outcomes:** `fresh-scaffold`

This content-safe receipt records the causal local certification and approved registry transition for the exact subject.

## Direct predecessor and authority

- Direct predecessor: the exact `portfolio@0.10.0` to `site@0.10.0` transition certificate delivered through merged pull request 55.
- Accepted predecessor revision: `main@b120929543f22e4476f162b01af7332d5678c0e1`.
- Predecessor acceptance check: successful post-merge Repository quality run `32910389499` at the accepted revision.
- The transition receipt's pre-squash evidence revision is historical evidence, not accepted-main ancestry after squash integration.
- Human authority: the user approved the subject plan, exact local journey, bounded environment adjustments, evidence acceptance, registry transition, final reviews, CodeRabbit corrections, and pull-request delivery in advance.

## Evidence command and environment

The exact clean revision ran:

```text
pnpm run verify:content-files-certification -- --revision f03b9f624c370728f678924ce34e5287558d2a87
```

The runner used repository-pinned Node.js `22.23.2` and pnpm `11.20.0`. Frozen dependency installation and Chromium installation used bounded network access inside identity-owned disposable roots. No credential, provider account, deployment, protected environment, persistent external state, or spend was required.

Two failed pre-evidence attempts were rejected and produced no accepted receipt: the first exposed unsupported package-script separator handling, and the second exposed an incorrect disposable fixture import. Both defects were corrected under a failing test or failing causal certification check, committed, and the complete journey reran at the recorded revision.

After the initial causal success, independent test-evidence review found three coverage gaps: ordinary-suite receipt binding, direct protection of the production fixture verifier's overlay and command sequence, and a YAML core-schema discriminator. The accepted validation core already enforced receipt binding; the normal builder suite now exercises it. The production verifier is now tested directly, and the generated unit fixture distinguishes core from JSON and failsafe schemas.

Final CodeRabbit review then found that support-root preparation, fixture-destination creation, and navigation-overlay writes could bypass the production verifier's specific opaque filesystem error. Three failing regression cases reproduced the raw failures before the runner mapped each to `CERTIFICATION_FIXTURE_OVERLAY_FAILED`. Because the runner and fixture are evidence inputs, the complete journey reran at the recorded revision after all corrections. Earlier revisions `591a6d2ee45ee02dd059e46dae7c6c2b1a10c96f` and `28b5769e42a9d7b3c6df47338536c56e630aafc4` are superseded and are not registry evidence.

## Fresh-scaffold outcome

The compiled CLI created a disposable `portfolio@0.10.0` named `acme-portfolio` with exact `content-files@0.4.0`. Fresh inference confirmed the subject, doctor returned healthy with no diagnostics, and exact diff returned equal with no differences.

The fixed generated-project verifier passed, in order:

1. pnpm version and frozen install;
2. peer-dependency, dependency-audit, and registry-signature checks;
3. lint, Cloudflare type generation, typecheck, unit tests, and component tests;
4. Next.js and OpenNext builds; and
5. Chromium installation plus development and workerd-preview browser journeys.

The capability-specific disposable overlay then passed a second frozen install, focused Vitest unit checks, Chromium installation, and a development-browser journey. The unit checks causally established YAML 1.2 core scalar behavior, including core-specific octal and null resolution, duplicate-key refusal, alias refusal, the exact `en-CA` configuration, valid Markdown front matter and body, and stable invalid-content refusal. The browser check causally established exact metadata, externalized skip-navigation copy, representative section copy, and navigation content derived from the generated YAML reader.

## Revision, cleanup, and privacy guards

The runner required the exact 40-character evidence revision, empty staged, tracked, and non-ignored-untracked status, and ordinary tracked Git index entries before and after the complete journey. It refused revision drift, worktree drift, hidden index flags, malformed arguments, invalid command output, invalid verifier identity, missing or reordered checks, fixture failure, and cleanup failure.

Only identity-checked local temporary owners were removed. The repository sources remained unchanged across the evidence run. No secret, environment value, private URL, machine-specific path, generated source dump, child-process output, registry response, browser profile, or raw log is retained here.

## Claim boundary

This receipt supports only the exact local `fresh-scaffold` outcome for `content-files@0.4.0`, behavior-contract digest `sha256:5ae35debef622dc0fb9eeee3889e79a72fd6ff28eb730865bfe95e8674c9ff05`, recipe `0.10.0`, locale `en-CA`, evidence revision, and recorded checks.

It does not establish translation quality, arbitrary content safety, visual or design quality, performance, deployment, provider behavior, publication, security certification, privacy completeness, production readiness, human accessibility, assistive-technology usability, or WCAG conformance. It does not certify `section-composition`, `site-routing`, another profile or recipe, an existing-repository lifecycle, or a removal path.

## Acceptance record

- Exact subject, digest, revision, recipe, locale, and evidence kind accepted: `yes`
- Compiled creation, state/inference agreement, diagnostics, and exact diff accepted: `yes`
- Fixed generated-project verifier result accepted: `yes`
- Strict parser and exact rendered-copy fixture result accepted: `yes`
- Cleanup, privacy exclusions, and claim boundary accepted: `yes`
- Registry transition approved: `yes`
- Review revision: `f03b9f624c370728f678924ce34e5287558d2a87`
- Rerun trigger: any material descriptor, required-evidence contract, runner, fixture, parser, content reader, generated output, verifier, or evidence defect requires a new clean evidence revision and complete affected rerun
