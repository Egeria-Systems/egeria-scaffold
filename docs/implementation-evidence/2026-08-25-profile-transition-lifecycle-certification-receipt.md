# Profile transition lifecycle certification receipt

**Date:** 2026-08-25

**Certification subject:** exact repository transition from `portfolio@0.10.0` to `site@0.10.0`

**Evidence revision:** `2650a9a5039f6b94d7d0c26dcc3d255ff6e6e579`

**Accepted implementation base:** `main@83dd4aa391af23106cc5479fafbeb184b89e4565`, containing merged pull request 53 and its accepted standards lifecycle certification

**Migration:** `transition-portfolio-0-10-0-to-site-0-10-0`

**Evidence command:** `pnpm run verify:profile-transition-lifecycle-certification -- --revision 2650a9a5039f6b94d7d0c26dcc3d255ff6e6e579`

**Result:** `passed`

This content-safe receipt records the successful local transition certificate. The user's advance verified-final-diff approval becomes effective only when the final unchanged candidate also passes the complete relevant verification, exactly three required independent reviews, hosted checks, and CodeRabbit with no unresolved actionable finding.

## Revision and runner binding

The runner required the exact 40-character evidence revision and an empty staged, tracked, and non-ignored-untracked Git status before and after both evidence commands. Revision or status drift, malformed arguments, a command failure, malformed TAP, zero selected tests, an absent expected name, or a selected skipped or todo test fails the certificate.

The certificate selected exactly seven compiled CLI tests from `apps/cli/tests/cli.test.mjs` and 27 operation-specific transition tests from `packages/builder-core/tests/apply-profile-transition.test.mjs`. It used the Node.js TAP reporter, parsed only unindented top-level results as selected evidence, tolerated nested results and unrelated skipped tests, and required the exact expected top-level passed-test order. Both selections passed.

No capability descriptor, behavior-contract digest, package version, provider outcome, or capability-certification registry record changed. This subject is the exact profile-transition edge, not a capability-package certification.

## Successful lifecycle outcome

The compiled CLI used the accepted committed generated portfolio fixtures in disposable Git repositories with dedicated attached linked worktrees. Repeated `plan-profile-transition` execution returned byte-identical plans and left Git plus all three `.egeria` controls unchanged. The plan bound the exact source/target recipes, catalogs, manifests, raw and parsed controls, desired/installed/inferred/inventory/ejection agreement, seven action bytes, public plan, and private Git identity.

`apply-profile-transition` consumed the exact approved fingerprint for both the representative portfolio and optional-Calendly portfolio. It applied exactly two creates and five replacements, verified an isolated VCS-free transformed copy, freshly inferred the pending site state, appended migration `transition-portfolio-0-10-0-to-site-0-10-0`, persisted state last, and stopped at `verified-final-diff-approval-required` only after post-state agreement, exact nine-path Git inspection, and exact changed-byte rereads.

The resultant state retained recipe `0.10.0` and schema `1.0.0`, changed the origin profile to `site`, recorded the exact migration in state and JSONL history, and set `lastSuccessfulVerification.kind` to `profile-transition` with only the completed persisted checks. Optional `booking-calendly@0.1.0` selection and settings remained byte-for-byte equivalent to the source intent.

## Refusal and no-mutation outcome

The compiled CLI evidence covered already-current site state, malformed target arguments, the finite unsafe planning matrix, wrong approved fingerprint, dirty linked worktree, primary checkout, and already-current application. The operation-specific suite additionally covered unsupported source/target and missing edge, ambiguous inference, incompatible or duplicate migration state, drift and ejection disagreement, managed-surface or create-target collision including ignored targets, stale or malformed plan authority, changed controls/action bytes/Git identity, and reader or preflight failures.

Every pre-write refusal returned its bounded public code and `not-required` recovery where applicable, exposed no repository metadata or private failure detail, and left the generated repository unchanged.

## Retained-prefix recovery outcome

A compiled CLI test replaced only the pnpm executable with a controlled failing fixture so ordinary verification failed after the exact transition actions were written. The command returned `PROFILE_TRANSITION_VERIFICATION_FAILED`, phase `verify`, and recovery `inspect-worktree`; transformed `.egeria/project.yaml` plus the six generated source/visual action paths remained at their exact target bytes, while `.egeria/state.json` and `.egeria/migrations.jsonl` retained their exact original bytes. The primary checkout remained unchanged and no private executable path leaked.

The operation-specific suite causally covered uncommitted and partial transformation writes; writer and verifier exceptions; verification and re-inference failure; clock, migration construction, append, and reread boundaries; state construction, persistence, replacement, and reread boundaries; post-state agreement; final Git inspection; and exact text or binary byte rereads. Each failure asserted the exact committed prefix, correct absence or presence of migration/state persistence, and either `not-required` before a committed write or `inspect-worktree` afterward. No test or runtime path performed automatic rollback.

## Environment, cleanup, and privacy

The certificate ran with repository-pinned Node.js `22.23.2` and pnpm `11.20.0`. Frozen dependency installation for disposable generated-project verification used bounded network access. The evidence commands removed only their identity-owned disposable repositories and failing-executable fixture after completion.

No credential, provider account, provider resource, workflow dispatch, deployment, publication, protected environment, production or staging state, persistent external data, secret value, private URL, machine-specific path, generated source dump, child-process output, registry response, or raw log is retained here.

## Claim boundary

This receipt supports only the exact local repository transition subject, evidence revision, edge, migration, selected compiled CLI journeys, operation-specific lifecycle tests, and recorded recovery prefixes. It establishes neither a generic transition framework nor another edge.

It does not establish capability-package certification, publication, deployed or provider behavior, ongoing availability, production readiness, security certification, privacy completeness, visual or design quality, performance, human accessibility, assistive-technology usability, or WCAG conformance. The successor `content-files@0.4.0` certification increment, P3 closure, P3B, and P3C remain separate.

## Acceptance record

- Local subject-bound certificate passed: `yes`
- Exact subject, revision, edge, migration, and command binding recorded: `yes`
- Successful, refusal, resultant-state, final-diff, and retained-prefix evidence recorded: `yes`
- Provider, deployment, publication, visual-quality, accessibility-conformance, and production-readiness claims excluded: `yes`
- Final delivery acceptance: `pending the unchanged final verification, three independent reviews, hosted checks, and CodeRabbit conditions stated above`
