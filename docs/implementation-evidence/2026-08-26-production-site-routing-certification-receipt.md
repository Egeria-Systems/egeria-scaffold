# Production Site Routing Certification Receipt

**Execution date:** `2026-08-26 America/Toronto (EDT, UTC-04:00)`

**Certification receipt status:** `complete`

**Certification reviewer decision:** `accepted`

**Certification unresolved prompts:** `none`

**Certification capability:** `site-routing`

**Certification descriptor version:** `0.4.0`

**Certification behavior-contract digest:** `sha256:17e62c4468bc05480828d23471b63afc29e19eb6a9bff07eee1f99d30cd7b3e3`

**Certification evidence revision:** `6034d7330af912d1a1b9bcff3323ed360ebee2d0`

**Passed certification outcomes:** `existing-repository-lifecycle, fresh-scaffold`

**Reviewed certification outcomes:** `existing-repository-lifecycle, fresh-scaffold`

This content-safe receipt records the causal local certification and approved registry transition for the exact current subject. The historical `site-routing@0.3.0` receipt remains a separate immutable record and does not certify this successor.

## Direct predecessor and authority

- Direct implementation predecessor: merged pull request 60 at `main@0a699d26198e94ddfaa596d812ad175284d05c49`, accepted tree `ac0e7eba8fc2890daf338db0a2cdecb5f47f6223`, and successful Repository quality run `33014687284`.
- Evidence branch base: refreshed descendant `main@a66890a6c30a275818e7b51f22ded987cc1d52ff`.
- Human authority: the user approved the exact subject, both required outcomes, bounded repairs and file-scope amendments, local commits, pull-request delivery, material CodeRabbit repairs, merge, and accepted-main verification in advance. The user separately authorized the disposable generated project's dependency names and versions to be sent to the npm registry advisory service for this run.
- Excluded authority: no deployment, provider action, publication, credential use, production mutation, or response to review comments was authorized or performed by this certification.

## Evidence command and environment

The exact clean revision ran:

```text
pnpm run verify:site-routing-certification -- --revision 6034d7330af912d1a1b9bcff3323ed360ebee2d0
```

The successful run used the repository-pinned `linux/amd64` Playwright `v1.62.1-noble` image at digest `sha256:dcc5531e97840b9b5e794f2814476b21571c5124a3fca2267d73041f56e7580e`, 1 GiB shared memory, Node `22.23.2`, pnpm `11.20.0`, and disposable Git `2.55.0`. The derived disposable toolchain image had manifest-list digest `sha256:29eae8956e818c3d38ec08bb05e5757914cb1e7e8ec7d02f61c15bccbe1fdb16`.

The official image's exact Playwright `1.62.1` Chromium `1234`, Chromium headless shell `1234`, and FFmpeg `1011` artifacts were copied into each verifier-created empty browser cache before the unchanged real pnpm and Playwright commands executed. This bounded environment repair preserved the verifier's per-run cache isolation while avoiding a redundant browser download that exceeded the command timeout. The real browser installer validated the exact artifacts, and the development, OpenNext/workerd preview, fixture, and visual browser journeys executed against them.

No failed or interrupted attempt is accepted evidence. A host-sandbox attempt failed closed when package-network access was unavailable. Docker Desktop was cleanly restarted after its local engine returned an unhealthy response. A first authorized container run timed out while redownloading Chromium into the verifier's intentionally empty cache. A second run was deliberately interrupted after process evidence proved that an initial wrapper did not reach Volta's nested pnpm launcher. The final repair targeted that exact launcher, passed a stripped-environment diagnostic matching the verifier's child process, and the complete unchanged certificate then passed.

The initial complete run at `f2b80d9e5e6bb08237d7cc887ee42d2f106e9243` was superseded after independent review found a pending target-evidence marker, stale current-status wording, and an incorrect predecessor tree claim. The reviewed repairs were rebased onto refreshed `origin/main`, and the complete certificate passed at `e69c28ec4228622fd34517a72858e2ac55401a5a`. Final evidence review then required the public upgrade endpoint to bind that accepted ancestor instead of the superseded initial run. After that focused RED/GREEN repair, the complete certificate passed again at `6034d7330af912d1a1b9bcff3323ed360ebee2d0`. The endpoint retains `e69c28ec4228622fd34517a72858e2ac55401a5a` as target provenance; only `6034d7330af912d1a1b9bcff3323ed360ebee2d0` is admitted as current registry evidence.

## Fresh-scaffold outcome

The compiled CLI created one disposable `site@0.11.0` project with exact `site-routing@0.4.0`, recipe `0.11.0`, and locale `en-CA`. Fresh inference confirmed the subject, doctor returned healthy with no diagnostics, and exact diff returned equal with no differences.

The fixed generated-project verifier passed exact pnpm version, frozen install, peer dependencies, dependency audit, registry signatures, lint, Cloudflare type generation, typecheck, unit and component tests, Next and OpenNext builds, browser installation, development and workerd-preview browser journeys, and retained deterministic visual comparison without updating baselines.

The subject-specific overlay then passed its own frozen install, component contract, browser installation, and development-browser journey. Those checks bound the exact installed subject and SHA-256 fingerprints for all seventeen owned surfaces; rendered the actual About, Featured Work, and not-found modules; verified direct and link-driven nested routes; exercised configured redirects; and checked route metadata, sitemap, robots, and not-found behavior.

Result: `passed`; human acceptance: `accepted`.

## Existing-repository lifecycle outcome

The compiled CLI and builder-core evidence exercised only the declared exact `site-routing@0.3.0` under historical `site@0.10.0` to `site-routing@0.4.0` under current `site@0.11.0` edge. Planning was read-only, selected the exact production-site actions and controls, and bound the approval fingerprint without writes.

Application recomputed and consumed that exact plan, transformed the approved production-site batch once, ran ordinary verification, freshly inferred the target subject, appended migration `upgrade-site-routing-0-3-0-to-0-4-0`, persisted state last, proved exact final control and source bytes, and stopped at `verified-final-diff-approval-required`. Focused failure evidence proved transformed source remains inspectable while old state and migration controls remain unchanged when verification fails; persisted-state corruption was rejected by the final awaited byte reread. Already-current and managed-drift repositories refused without writes.

Result: `passed`; human acceptance: `accepted`.

## Revision, cleanup, and privacy guards

The certifier required the exact 40-character revision, empty staged, tracked, and non-ignored-untracked status, and ordinary tracked Git index entries before and after all evidence. It required the explicit TAP reporter, exact selected lifecycle test names and counts, both required outcomes in canonical order, the exact verifier and fixture check sequences, and unchanged repository sources.

The runners removed their identity-owned disposable generated roots after success. The standalone clean clone and disposable toolchain image were retained locally through downstream review and accepted-main verification; they contain no credential or provider state and are not additional evidence. No secret, environment value, machine-specific path, private URL, generated source dump, child-process output, advisory response, browser profile, screenshot, or raw log is retained here.

## Claim boundary

This receipt supports only the exact local `fresh-scaffold` and `existing-repository-lifecycle` outcomes for `site-routing@0.4.0`, behavior-contract digest `sha256:17e62c4468bc05480828d23471b63afc29e19eb6a9bff07eee1f99d30cd7b3e3`, site recipe `0.11.0`, locale `en-CA`, the declared `0.3.0` to `0.4.0` edge, the evidence revision, and the recorded checks.

It does not establish another capability version, recipe, profile, edge, removal lifecycle, automated recovery, deployment, provider behavior, publication, production readiness, visual or design quality, arbitrary responsive quality, performance, broad security certification, privacy completeness, translation quality, human accessibility, assistive-technology usability, or WCAG conformance. Screenshot equality is narrow regression evidence only.

## Acceptance record

- Exact subject, digest, revision, profile, recipe, locale, edge, and evidence kinds accepted: `yes`
- Compiled creation, state/inference agreement, diagnostics, and exact diff accepted: `yes`
- Fixed generated-project, audit/signature, browser, workerd, and retained visual results accepted: `yes`
- Exact ownership, route, content, metadata, redirects, crawl output, and not-found fixture results accepted: `yes`
- Planning, application, verification-failure prefix, migration-before-state, exact final state, and final-byte reread accepted: `yes`
- Environment amendments, superseded-attempt dispositions, cleanup, privacy exclusions, and claim boundary accepted: `yes`
- Registry transition approved: `yes`
- Review revision: `6034d7330af912d1a1b9bcff3323ed360ebee2d0`
- Rerun trigger: any material descriptor, required-evidence contract, edge, runner, fixture, lifecycle implementation, verifier, generated output, visual baseline, registry, or evidence defect requires a new clean evidence revision and complete affected rerun
