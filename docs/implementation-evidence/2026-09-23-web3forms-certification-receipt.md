# Hosted contact form certification receipt

**Certification capability:** `contact-form-web3forms`
**Certification descriptor version:** `0.1.0`
**Certification behavior-contract digest:** `sha256:e462b6432eaac905b973d2122c122abca5fdd498e262ee63f7f2508b16978be9`
**Certification evidence revision:** `94c28901fb4388f771df53cc18cf2eec24b76215`
**Passed certification outcomes:** `cleanup-recovery, deployed-application, existing-repository-lifecycle, fresh-scaffold, provider-confirmed`
**Reviewed certification outcomes:** `cleanup-recovery, deployed-application, existing-repository-lifecycle, fresh-scaffold, provider-confirmed`
**Certification receipt status:** `complete`
**Certification reviewer decision:** `accepted`
**Certification unresolved prompts:** `none`

## Subject and acceptance boundary

Execution occurred on 2026-09-23 against the accepted PR #142 merge revision above. [Current-main quality run 35890127289](https://github.com/Egeria-Systems/egeria-scaffold/actions/runs/35890127289) passed every applicable job; dependency review was inapplicable to the push. The approved preparation candidate and merged source have equal trees. No source, descriptor, template, fixture or installed capability was changed during the live run.

Independent outcome review found no material defect in the exact evidence and proposed transition. The repository owner explicitly confirmed the message reference, name/message mapping and approved Reply-To, and accepted all five outcomes, retained-resource limitations and the exact final change before the registry transition. The [review protocol](../governance/review-and-contribution.md) owns acceptance; the [capability model](../architecture/capability-model.md#hosted-contact-form-boundary) owns the exact behavior boundary.

## Fresh-scaffold outcome

[Protected deployment run 35905133285](https://github.com/Egeria-Systems/egeria-scaffold/actions/runs/35905133285) generated a new `site@0.12.0` through the compiled CLI. Its normalized receipt binds the exact subject and source revision to the seven selected capabilities, valid state, confirmed contact inference, healthy diagnostics and equal managed diff. It passed frozen installation, peer checks, dependency audit and signatures, lint, Cloudflare type generation, typecheck, unit/component tests and OpenNext build.

Installed packages were Next `16.3.3`, OpenNext `1.20.2`, React `19.2.8`, Vitest `5.0.0`, Playwright `1.62.1` and Wrangler `4.118.0`. The unchanged contact suite passed in pinned Linux/Chromium against workerd with provider traffic intercepted. Those cases establish controlled application behavior, not live provider enforcement.

Accepted all-profile implementation evidence is retained from tested revision `14322daecec62ee9a1fcf352ea9a6e5419e012d5`, integrated as `117f4c8a5d8563923d68efe2738fa57babfb7dfd` with the same tree. Of its 157 manifest paths, 155 remain byte-identical at this receipt's revision; only two roadmap documents changed. Current-main quality renewed affected CLI and generated-project verification after the intervening dependency changes. Historical fixtures were preserved.

## Existing-repository lifecycle outcome

The accepted implementation exercised compiled contact addition, removal and re-addition, edited-copy preservation, surviving-reference refusals, interrupted writes and coexistence with installed capabilities. Current-main run 35890127289 independently passed all 97 CLI tests, including compiled Web3Forms lifecycles on portfolio and app and strict public-identifier handling. The contact implementation inputs match the accepted predecessor; the current dependency inputs were exercised by this renewed run. Local lifecycle removal makes no provider or mailbox deletion claim.

## Deployed-application outcome

The protected deployment run passed its deployed site-quality and routing checks on the reusable non-production `test-deploy` Worker. Cloudflare readback matched version `7308edf5-1182-46de-a613-364693b8d2da` at 100 percent traffic. GitHub Actions was the sole deployment authority.

In the connected Brave browser, the generated home displayed the contact fields, privacy notice, existing contact-link fallback and activation-only hCaptcha. About had no form. The generated fallback retained its placeholder email destination; no fallback email delivery is claimed.

The human completed real CAPTCHA and submitted the prefilled synthetic message during handoff. Subsequent browser readback showed acknowledged acceptance and cleared fields. The human independently reported receiving the email appropriately. The agent did not access the mailbox or read, store or replay a CAPTCHA token. The normal attempt was recorded retrospectively because the human submitted during handoff; its exact send time was not observed. Delivery was reported within the bounded observation window, without automated polling or retry.

## Provider-confirmed outcome

One normal submission and two separately approved negative probes were executed, within the five-attempt ceiling. Each negative probe used a private loopback document with one native POST form to the fixed official endpoint. The missing-token probe returned a CAPTCHA-required rejection; the invented-invalid-token probe returned a CAPTCHA-validation rejection. Both were recorded before their single actual submit actions. No real token was read or reused, and no automatic retry occurred.

The provider dashboard remained at one accepted submission out of 250, with hCaptcha selected, Basic spam filtering enabled, autoresponder disabled and seven-day retention. The normal message's opaque reference is `W3F-35905133285-NORMAL-01`. The human independently confirmed receipt of this reference, correct name/message mapping and the approved Reply-To address. These observations cover one owned form and do not establish general abuse resistance or ongoing availability.

## Cleanup-recovery outcome

[Protected recovery run 35906841858](https://github.com/Egeria-Systems/egeria-scaffold/actions/runs/35906841858) restored the compatibility baseline at the same approved source revision and passed its deployed tests, including the expected compatibility/workerd runtime report. Cloudflare readback matched version `31760041-b470-4bca-be11-71033cb91a1f` at 100 percent traffic.

Brave showed the compatibility homepage without contact and rendered 404 pages at `/about` and `/work/featured`. Numeric HTTP status codes were not independently captured by that manual browser check. The earlier Brave block of the runtime-report URL was not bypassed; successful runtime-report evidence comes from the separately authorized protected workflow.

Final dashboard readback showed no Worker secrets, only the ASSETS resource binding and PROOF_ENVIRONMENT=compatibility, disabled Logs and Traces, no cron/queue/email triggers, no custom domains or zone routes, and retained workers.dev and preview URLs. The final displayed request quota was 339/100,000; this is an account observation, not an exact count attributable to this journey. No upgrade or incremental-spend action occurred.

The loopback server stopped; its listener was verified closed. Only its two identified private payload files and empty owned directory were removed. Synthetic submission tabs were closed. Reusable Worker, form, GitHub environment, public identifier and existing credentials remain under the human owner's approved retention and scope-review decision. Provider settings retain submissions for seven days; mailbox copies remain human-controlled. Provider backups/logs, hCaptcha processing, historical Worker versions and preview URLs may retain data or the public identifier. No universal erasure, credential rotation or deletion is claimed. Credential expiry remains unspecified; the owner must review scope and expiry before future use. The shared Worker lease was released after recovery and disposition readback.

## Evidence and claim limits

Only the bounded normalized preparation receipt was downloaded from the deployment workflow. Private normalized run, version, attempt and recovery records preserve provenance; raw provider responses, email bodies/headers, credentials, public form identifiers and CAPTCHA tokens are excluded from this receipt. Hosted artifacts follow their configured retention; separately retained private evidence supports later inspection.

This receipt concerns only the exact subject above. It does not certify Resend or any other pending capability, close the app-foundation phase, establish exactly-once delivery, production readiness, security/privacy completeness, translation quality, human accessibility or WCAG conformance. Source removal, baseline restoration, provider-data disposition and credential recovery remain separate outcomes.
