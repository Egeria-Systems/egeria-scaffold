# Program Roadmap

This is the concise execution sequence. It does not supersede the [full approved source plan](2026-08-04-nextjs-boilerplate-builder-best-reconciled-plan.md) or an accepted ADR.

## Contact and portability work deferred

**User decision — 2026-09-24:** Defer durable contact submissions (P5F-1–4 and contact certification tasks P5F-C0, P5F-CP and P5F-C) and the alternate-adapter/package-support experiment (P10-4) until separately reactivated. Their original requirements and identifiers remain available in the [delivery plan](2026-09-16-remaining-program-delivery-plan.md). They are outside the active delivery sequence and its completion obligations, not implemented, certified, cancelled or deleted.

Every other increment remains required: existing P4 and P5C/D/E acceptance and certification duties; P6; the complete P7 recipe including Google and support-console; P8A/B and every P8C mode and migration; P9-1/2; and P10-1/2/3/5. Web3Forms, the environment stream and ADR-0020 activation gates, existing deferrals, and capability-owned security, privacy, deployment and recovery requirements are unchanged. Keep conditional contact views dormant until a supported, certified contact capability exists; the retained account/support baseline and later billing views remain required.

The site dependency-only persistence extension still needed by retained payments transfers to [P8C-1](2026-09-16-remaining-program-delivery-plan.md#p8c-1--deliver-reconciled-one-time-payments), with separate exact-subject acceptance and its strict future admission gate. Portfolio widening for contact stays deferred. This transfers planning ownership, grants no certification or composition exception, and does not restart P5C. [ADR-0004](../adr/0004-cloudflare-isolation.md) isolation and in-memory/contract tests, [ADR-0005](../adr/0005-evidence-driven-package-extraction.md) extraction controls, and measured package reassessment in P10-1/2 remain required; only the alternate-adapter experiment and its contingent support/extraction decisions are deferred.

Future final approval may accept the amended active delivery scope only after every retained obligation and applicable executable certification gate is satisfied, with these two deferrals and their limitations explicit. It cannot mark P5F, P10-4 or the original entire program complete. Existing pending registry records and `all-certified` semantics remain unchanged; this scheduling decision excuses no executable subject or missing prerequisite.

Reactivation requires an explicit user request, a demonstrated contact workflow or alternate-adapter/portability need, fresh canonical and dependency review, exact implementation/evidence scope and the usual approvals. It is not automatic at the next phase or final hardening. Contact preparation must discover and reuse whatever exact persistence support has actually been accepted, without repeating completed widening because an older card still names it. Preserve all existing source, evidence, fixtures and external resources.

## P0 — Architecture materialization and compatibility proof

### P0.1 — Constitution and ADRs

Create the private root pnpm workspace, substantive root `AGENTS.md`, architecture overview, capability catalog, enforcement map, roadmap, accepted ADRs, and review protocol. Future capabilities are visible but not implemented. No application, package, `.egeria` state, dependency, lockfile, workflow, or Cloudflare resource is created.

**Exit:** All authoritative decisions are represented without contradiction; enforceable rules have an actual or named planned gate; no speculative runtime package exists.

**Stop gate:** Present the P0.1 review packet and obtain explicit verified-final-diff approval before planning P0.2.

### P0.2 — Deployed compatibility proof

Build the smallest Next.js/Cloudflare proof that validates the approved Volta Node.js pin and current compatible pnpm, Next.js App Router, OpenNext Cloudflare adapter, local Next development, production-like workerd preview, generated binding types, strict TypeScript, ESLint flat config, ordinary Vitest, Cloudflare integration testing, Playwright, and axe. Deploy only to an approved non-production environment through GitHub Actions. Record current official documentation, advisories, beta status, known limitations, and runtime distinctions.

**Completed (2026-08-04):** The locked proof passed Node.js development, built-Worker harness, workerd preview, shared Chromium/axe smoke, clean Ubuntu workflow, non-production Cloudflare deployment, and independent deployed-smoke evidence for exact implementation commit `160b8ef`. Required reviews were reconciled, and the user approved final committed comparison `c90b7c2..818874b`. See the [compatibility record](../compatibility/nextjs-cloudflare.md).

**Exit:** The exact locked combination works locally, under workerd, in tests, and at the non-production URL. Static or local success alone is insufficient.

**Stop gate:** Present the P0.2 review packet and deployed evidence; obtain approval before P0.3.

### P0.3 — Lean builder monorepo

Create thin `apps/cli`, the private `packages/builder-core` package and its ownership boundary, public `packages/standards`, the public observability package shell, and approved release tooling with accidental-publication safeguards. Reserve project/state schema ownership for `builder-core`, but defer the executable schemas and builder kernel to P1. Do not create a separate project-schema package.

**Completed (2026-08-05):** The lean ownership shells, consumed standards APIs, internal ESLint 10 boundary, dual ESLint-major tests, and local release safeguards passed the complete P0.3 and affected P0.2 verification. Required reviews were reconciled, and the user approved final committed comparison `40604eb..da74a5b`. P1 preparation and exact-file planning are authorized; P1 implementation remains separately plan-gated.

**Exit:** Package APIs and owners are explicit, publication safeguards pass, extraction decisions are justified, and no profile functionality is implemented.

**Stop gate:** Present the P0.3 review packet and obtain approval before P1.

## P1 — Builder kernel

P1 is the first executable project/state schema stage. Implement private builder-core schemas, capability resolution, installed manifests, `.egeria` state contracts, read-only inference/doctor/diff, deterministic portfolio/site skeleton generation, hybrid ownership, and fixture infrastructure.

**Completed (2026-08-09):** The bounded six-capability catalog, portfolio/site materialized recipes, state and ownership contracts, read-only inspection, state-last new-directory generation, thin CLI commands, committed deterministic fixtures, and isolated fresh-install/build verification passed the final schema-contract and Gate 3 reviews. The user approved exact final committed comparison `303ee9d35e19f9191948d994159f77c82c90a1ed..5580da10eded51ceefa53a068c7ddaaddf2a2d50`.

**Stop gate:** Generated skeletons build, manifest and inference agree, and the P1 review packet is approved.

## P2 — Client-ready portfolio

Deliver an accepted client-ready one-page portfolio baseline generated by the actual builder, with validated YAML/Markdown content, externalized copy, bounded sections, responsive accessible UI, Calendly link/inline/popup with fallback, observability, CI/deployment, visual checks, and automated accessibility gates, within the recorded claim limits and without a production-readiness claim.

**Completed through unnumbered closure amendment (2026-08-21):** Task 1 added versioned validated YAML/Markdown content contracts and deterministic generated portfolio/site evidence. Task 2 added standards-owned static visible-copy enforcement for canonical templates. Task 3 materialized the source-owned typed section catalog. Task 4's responsive Tailwind presentation, semantic design tokens, visible focus/reflow foundations, minimum primary-link targets, reduced-motion protection, and externalized skip-navigation copy are approved at committed artifact `e7026bd9e8c7a7ca20b5a485ee6702d2921a7586`. Selecting Task 5 as the next increment approved Task 4B's generated browser-quality foundation at committed artifact `02ec5eb12741c1622beec02529c38965e7501d68`. Selecting Task 5B as the next increment accepted Task 5 Calendly initial scaffolding, and Task 5B's bounded provider certification evidence is complete. Task 6's exact implementation diff `717c3bb0f048f4a4bc544100125ae42d818f09bc..45b57d2dc265ef6ba9ac805d7352a01db5f1081d` is approved and the implementation task is complete. The explicitly amended `observability@0.2.0` certification candidate records reviewed deployed-application and fresh-scaffold evidence from successful workflow run `31664542523`. `cleanup-recovery` is not claimed, and the retained-resource disposition is accepted without validating selected user-retained resources. Task 6C is integrated at `main@12ecc73a8337ab12ece9dd3a6b2aec03f940383c`; its exact content candidate `93e4e9f6ea944329de7c47c9e8bf34382774b1f8` passed local verification and all three applicable hosted workflows before integration. Task 6D is squash-integrated at accepted `main@c9294e9dc59d4b7bafed406846af3b43a10733d3`, and accepted repair `ee1e1df10fa2be2f09333efecd86de7f7a131d49` binds the reviewed rerun receipt to accepted-main evidence revision `c9294e9dc59d4b7bafed406846af3b43a10733d3`. Automatic-CI Plan A is integrated at accepted `main@368b9491fd2f813f83f1e456823d8c7546f6762c`. Task 6D evidence renewal is integrated at accepted `main@7b5324cfcffc7eb94f48cc304cbfe0ceb08c3486` and binds all eight independently reviewed outcomes to descendant `d7c63b0aaa9bebd56c075f16f1e5d86519853698`. No hosted workflow was dispatched for Task 6D renewal. Those `observability@0.2.0` milestones remain historical evidence for that exact prior subject. The restricted-error-diagnostics implementation revision `393225988aaed173e21dc547e69ff5b03305cf93` is integrated by accepted-main merge `d543de78d8e1c238a499aeba5e315f4db724dd1b`. Exact `observability@0.3.0` is now certified from accepted fresh-scaffold, deployed-application, and cleanup-recovery evidence at revision `bdcc55f1bfa6eca392ce3e36bdc35adb6f085bad`, diagnostics run `31925083913`, and recovery run `31925927776`. Task 7's generated protected Cloudflare deployment implementation is integrated by accepted-main merge `79dda83cd5ee848ce59689fc03c439c972c9fe84` and reconciled in accepted source at `main@601bc515e6e88c3e3df9f669b621658a2bbea796`. Task 7B separately certified that exact deployment subject. Three hosted quality attempts at its unchanged generated revision reproduced the GitHub runner package-mirror stall, including on runner image `ubuntu24/20260816.277`. The readiness-amended generated revision `32592cf84ceee7f23d6fcdcf0b73c171248fd2b4` later passed generated quality and exact deployment run `32301401594`, including deployed browser/axe checks, but that evidence was superseded before acceptance when review changed both Chromium workflow bytes. The final shared-memory generated revision `47eb9ef09ea412dbfcf14f69dda153fb14a0691b` passed automatic quality run `32315057638` and exact deployment run `32315740366`, including bounded readiness and all seven deployed browser/axe checks. Direct retained-baseline readback found current version `78387f27-0c4c-46e1-bcdb-ffebd0928e12` at 100 percent traffic, zero errors, only the declared bindings, and no custom domain or route. The accepted three-outcome receipt and registry transition at `718ae58f375099a92223618137f3853d735f860b` were merged by pull request 33 at accepted `main@b46f5f59c7f98ed6be1fa569a2f4a1f23d1ca1ad`; post-merge Repository quality run `32323617228` passed every applicable job. Current `main` therefore records exact `deployment-cloudflare@0.3.0` as certified at builder evidence revision `ea5a8ae8a6b0aa5fd7b8bc3bab3e03a52242aee2`. Further production action and successor work remain separate.

Task 8 deterministic visual regression is integrated at accepted `main@8e5f376f32a95f87420fd82a61566c08c2db020e` for four representative portfolio/site OpenNext screenshots under recipe `0.10.0` and exact `standards@0.4.0`; post-merge Repository quality run `32399819237` passed every applicable job for that exact revision. That run is accepted-main integration evidence, not Task 8B certification evidence. Task 8B is complete, and exact `standards@0.4.0` is certified from accepted local `existing-repository-lifecycle` and renewed `fresh-scaffold` evidence at revision `d7f9dac6e25d5dde32015968d0912b45e73644e7`, with no evidence carried forward from `standards@0.3.0`. Under the unnumbered closure amendment, performance budgets are deferred and no performance claim is made; neither controlled-laboratory nor field or real-user performance evidence is established. P2 closes without changing the accepted Task 8 or Task 8B evidence, and the visual regression claim limits remain unchanged. The archived performance experiment is not canonical source, accepted evidence, or a merge input. P3 is eligible only after verified-final-diff approval and accepted-main integration of this amendment; P3B must not begin before P3 closes. Screenshot equality does not establish visual or design quality, human accessibility, assistive-technology behavior, deployed behavior, production readiness, or WCAG conformance. The real client project, analytics, workflow dispatch, deployment, and provider mutation remain outside this amendment.

The closure amendment is integrated at accepted `main@94a1d88f500b145366e065797633788a415c00ef`, satisfying the P3 entry gate. The first transactional-lifecycle implementation base is accepted `main@7ba461ac20d4a1d708e9f7b940e15cda0fce3295`; post-merge Repository quality run `32429352322` passed every applicable job. The exact Calendly addition-execution predecessor is accepted `main@31e1bab38496c87dc2e6084c958bd9300a141508`; post-merge Repository quality run `32577925329` passed every applicable job. The currently implemented boundaries are read-only `plan-add`, fingerprint-gated `apply-add`, read-only `plan-remove`, and fingerprint-gated `apply-remove` for exact `booking-calendly@0.1.0`; application performs existing-repository transformation, verification and re-inference, migration append, state-last persistence, and exact final-diff inspection before stopping for verified-final-diff approval, while removal planning classifies ownership-aware dispositions and produces a redacted fingerprinted approval plan with no write. Every removal plan requires repository-wide surviving-reference review and exact-path reconciliation of any preserved/ejected capability surfaces before transformation approval; the current planner declares but does not automate those reviews. Merged pull request 46 integrates exact removal execution at accepted `main@7f59e8b093edb7be617cd2a30bfb4ebaa6a8ab6e`, and post-merge Repository quality run `32620215344` passed every applicable job. Accepted read-only `plan-upgrade` and exact accepted `apply-upgrade` add only the single standards edge described in P3 below. Merged pull request 49 integrates read-only `plan-profile-transition` for exact `portfolio@0.10.0` to `site@0.10.0` at accepted `main@612a963ab96221837b1c8ac815f41e90736d292e`; reviewed head `d0ad744e3818046f755d3933843b22213307c109` and accepted main share tree `85afdf22ea8625f3b70cdd712f961d629e948daa`, and post-merge Repository quality run `32745968642` passed every applicable job. Merged pull request 50 accepts the exact executor at `main@641db9537f5dea4911b0b727eb083f8d6d359204`; reviewed head `c41845597c63899d017d448791de874c1ef7d999` shares tree `0e3c780b517f0cddac7ff1f31ef2c7f32597e6bb`, and Repository quality run `32766917652` passed every applicable job with CodeRabbit successful. Merged pull request 51 accepts the private control-persistence extraction at `main@532a7cd6e874db13ac8c4b1d2f376abe83862772`. Exact `booking-calendly@0.1.0` lifecycle certification is complete: causal local fresh-add and compiled lifecycle evidence is bound to `b30e10b86b9ac9ef8dfdf1e8fa8e4077e2abe059`, and protected-staging/provider/cancellation/cleanup evidence is bound to `f9bd78f115c2118afd6dcc17ce49b2bfe34ca10d`; clean-main restoration run `32823960628` returned the shared Worker and environment policy to baseline. A generic lifecycle executor, another upgrade or profile-transition edge, automated recovery, unrelated certification backfill, P3B, and P3C runtime work remain planned or separately gated. P3B must not begin before P3 closes.

**Historical Task 6C handoff (superseded by the integration status above):** Under the explicit 2026-08-11 independent-work exception, Task 6C has an independently reviewed implementation candidate on merge request 2, reconciled with `main@2a315aa0e7dce1bf1048b9a2c07e318add9241de`. Content candidate `93e4e9f6ea944329de7c47c9e8bf34382774b1f8` passed local verification and all three applicable hosted workflows. Task 6C awaits verified-final-diff approval and one eligible repository approval. This paragraph preserves the exact pre-integration handoff consumed by the existing constitution contract; it is historical evidence, not current status.

The current accepted baseline has two executable profiles, `portfolio` and `site`, at recipe `0.10.0`, and seven executable capabilities: `standards`, `content-files`, `section-composition`, `deployment-cloudflare`, `observability`, `site-routing`, and `booking-calendly`; exact `standards@0.4.0` is certified from accepted local `existing-repository-lifecycle` and renewed `fresh-scaffold` evidence at revision `d7f9dac6e25d5dde32015968d0912b45e73644e7`. Task 7 materialized `deployment-cloudflare@0.3.0`, adding the generated protected manual workflow to its exact OpenNext/Wrangler/configuration ownership. The workflow binds one exact `main` revision, uses the fixed GitHub `production` environment and read-only repository permission, verifies frozen source before its sole credential-bearing deploy-only step, and runs the strict HTTPS deployed browser smoke afterward. The Task 7B amendment keeps those semantics and moves both generated browser jobs to the exact official Playwright image matching installed Playwright at an immutable digest, installing only Chromium because the image supplies browser system dependencies and bounding each container's shared-memory allocation at 1 GiB. Task 7B owns the separate fresh-scaffold, deployed-application, and cleanup-recovery certification evidence. Three initial quality attempts reproduced the hosted package-mirror stall. Deployment run `32287049126` then passed through the credential-bearing deploy step before the first deployed-browser request received a temporary provider `404`; the same Worker root returned `200` seconds later. The readiness-amended exact generated main `32592cf84ceee7f23d6fcdcf0b73c171248fd2b4` subsequently passed generated quality and deployment run `32301401594`, including readiness and deployed browser/axe checks, but those workflow bytes were superseded before evidence acceptance. The final shared-memory exact generated main `47eb9ef09ea412dbfcf14f69dda153fb14a0691b` passed automatic quality and exact deployment run `32315740366`; provider and retained-baseline readback completed the accepted `deployed-application` and `cleanup-recovery` outcomes. The registry now records the exact subject as certified from all three accepted outcomes, and merge `b46f5f59c7f98ed6be1fa569a2f4a1f23d1ca1ad` plus post-merge Repository quality run `32323617228` establish its accepted-main integration. The retained Worker and prior runs remain provider state or bounded diagnostic/superseded evidence only. No production-readiness or WCAG-conformance claim is established. Further provider mutation, production action, and successor work remain separate.

Default `standards@0.4.0` adds exact generated Vitest/Testing Library dependencies, named Node unit and jsdom component projects, semantic run/watch scripts, actual parser and presentation starter specifications, deterministic visual-regression surfaces, explicit generated quality-workflow steps, and state/inference ownership while retaining exact public package `@egeria-systems/standards@0.1.0`; its accepted local `existing-repository-lifecycle` and renewed `fresh-scaffold` evidence certify that exact subject at revision `d7f9dac6e25d5dde32015968d0912b45e73644e7`. Default `observability@0.3.0` installs exact public package `@egeria-systems/observability@0.3.0` and generates bounded Next.js server/browser capture, a strict same-origin ingestion route, web-vitals reporting, selected caught-error reporters, and five error UI/content surfaces. Workers custom records receive only bounded safe operational events; the optional Better Stack diagnostic adapter alone receives bounded restricted error reports, and provider portability remains an adapter/composition property. Message and stack sanitization reduces exposure but is not a privacy guarantee, and stack strings are not source-map deobfuscated in this increment. Better Stack requires the declared server-held host/token secrets; Cloudflare Web Analytics remains absent. Explicit paired initial-scaffolding arguments continue to materialize `booking-calendly@0.1.0`.

Current generated-testing evidence covers exact production regeneration, state/inference agreement, frozen install, peer/audit/signature and Wrangler-type checks, lint/typecheck, unit/component tests, one standalone Next build followed by the OpenNext `--skipNextBuild` transform, and development/direct-workerd-preview Chromium checks for all three retained variants. One automatic read-only workflow exposes stable `scope`, `builder-and-packages`, `generated-projects`, `compatibility-proof`, and pull-request-only `dependency-review` jobs; validated job-level scoping is fail-safe, and reusable pnpm caches are disabled. The always-on read-only builder/package workflow and the path-scoped generated-project and compatibility-proof workflows passed hosted runs `31583624246`, `31583624387`, and `31583624223` for exact content candidate `93e4e9f6ea944329de7c47c9e8bf34382774b1f8`. Those predecessor hosted runs remain historical evidence and do not establish hosted execution or required-check configuration for the consolidated workflow or the new manual generated deployment workflow. Automated results remain bounded evidence and no visual, performance, human-accessibility, or conformance claim is made.

Calendly account/event configuration, provider scheduling data, cookies, retention, and cleanup remain unmanaged. The historical observability receipt establishes Better Stack delivery and aggregate Workers Logs evidence only for the exact prior `observability@0.2.0` run; exact `0.3.0` certification uses only its own accepted evidence. The browser ingestion route remains origin-checked, strictly bounded, and unauthenticated. `waitUntil()` remains best-effort and non-durable, Cloudflare platform/framework logs remain separate provider-controlled records, and retention and quotas remain plan-dependent. Current `0.3.0` certification proves the exact bounded deployed/provider journey and recorded cleanup/recovery only; it does not establish durable delivery, ongoing provider availability, privacy completeness, performance, visual quality, human accessibility, general production readiness, or WCAG conformance. Source, package, credential, deployment, provider, and data recovery remain separate domains.

The prior `booking-calendly@0.1.0` digest remains historically certified from initial-scaffolding, protected-staging, provider-confirmed, cancellation, and cleanup evidence. The active current digest adds `existing-repository-lifecycle` and is certified from the reviewed fresh compiled-add lifecycle plus separately bound protected-staging, provider-confirmed, cancellation, and cleanup outcomes. Neither historical nor current evidence establishes ongoing provider availability.

**Development stream:** Automatic-CI Plan A is integrated at accepted `main@368b9491fd2f813f83f1e456823d8c7546f6762c`. The separate Task 6D evidence renewal passed all eight outcomes at descendant `d7c63b0aaa9bebd56c075f16f1e5d86519853698` and is integrated at accepted `main@7b5324cfcffc7eb94f48cc304cbfe0ceb08c3486`. The current workflow-runtime evidence-only renewal binds the unchanged certified standards subject to independently reviewed revision `ea5a8ae8a6b0aa5fd7b8bc3bab3e03a52242aee2`. The prior `observability@0.2.0` certification transition is historical. The restricted-error-diagnostics implementation is integrated by accepted-main merge `d543de78d8e1c238a499aeba5e315f4db724dd1b`. The separate `observability@0.3.0` certification transition is integrated at accepted `main@cafcd2b65ab2567682c1fd9d700ef4159f7fde98`; tracked accepted receipt `docs/implementation-evidence/2026-08-16-observability-error-diagnostics-certification-receipt.md` binds exact evidence revision `bdcc55f1bfa6eca392ce3e36bdc35adb6f085bad`. Task 7 is integrated by accepted-main merge `79dda83cd5ee848ce59689fc03c439c972c9fe84`. Task 7B's accepted certification transition at `718ae58f375099a92223618137f3853d735f860b` is integrated by pull request 33 at accepted `main@b46f5f59c7f98ed6be1fa569a2f4a1f23d1ca1ad`, and exact post-merge Repository quality run `32323617228` passed every applicable job. Task 8 deterministic visual regression is integrated at accepted `main@8e5f376f32a95f87420fd82a61566c08c2db020e`, and exact post-merge Repository quality run `32399819237` passed every applicable job. The retained repository, environment, token, and Worker remain reusable certification resources; no additional workflow dispatch, provider mutation, cleanup, production action, or successor work is implied. This does not alter the isolated-worktree requirement for builder commands that transform generated client repositories.

Historical development-stream handoff: Task 6C remains on its approved isolated merge-request branch/worktree and has reconciled accepted `main@2a315aa0e7dce1bf1048b9a2c07e318add9241de` under the [review and contribution protocol](../governance/review-and-contribution.md). That statement records the pre-integration comparison only and is superseded by the current Plan A development-stream paragraph.

**Capability-certification history and current task:** Historical P2 Task 6B is the separate `observability@0.2.0` certification increment, and that certification increment is complete. A private protected-staging receipt binds the reviewed `fresh-scaffold` and `deployed-application` outcomes to revision `ee1e1df10fa2be2f09333efecd86de7f7a131d49` for that exact prior subject. `cleanup-recovery` was not claimed; the retained-resource disposition is accepted by explicit user decision and remains operationally uncertified. Diagnostics Task 5 was selected as the next increment after the certification transition was integrated. Exact `observability@0.3.0` and `deployment-cloudflare@0.3.0` remain bound to accepted exact-subject evidence; prior-digest `booking-calendly@0.1.0` receipts remain historical while its active lifecycle digest is task-linked and certified from its exact current receipts. The active-digest `booking-calendly@0.1.0` certification completed its protected workflow/provider journey and cleanup; that bounded certification did not authorize production action or a successor increment. The independently reviewed `standards@0.3.0` workflow-runtime renewal remains historical evidence only for that exact prior subject. Task 8B is complete, and exact `standards@0.4.0` is certified from accepted local `existing-repository-lifecycle` and renewed `fresh-scaffold` evidence at revision `d7f9dac6e25d5dde32015968d0912b45e73644e7`. Exact `content-files@0.4.0`, `section-composition@0.3.0`, and `site-routing@0.3.0` are certified from their accepted local `fresh-scaffold` evidence, with site-routing bound to revision `77cea944513e521939bf4de088048f67acdfbc3c`. Exact `multilingual@0.1.0` is certified from accepted local `fresh-scaffold` and `existing-repository-lifecycle` evidence at revision `96b587a254cf6fc859867d6fc66c7e0c900c4cfd`. Exact `analytics@0.1.0` is certified from accepted `cleanup-recovery`, `deployed-application`, `existing-repository-lifecycle`, `fresh-scaffold`, and `provider-confirmed` evidence at revision `a97341ea628210b6fa713fb12461084f20c3f8da`. Those accepted receipts remain historical evidence only for their exact prior descriptor digests. The current [`certifications/capabilities.json`](../../certifications/capabilities.json) registry admits ten coordinated pending subjects, the private plan gate passes, and `all-certified` closure rejects only those ten pending subjects.

**Stop gate:** The unnumbered closure amendment closes the accepted generated portfolio baseline and launch scope within the recorded claim limits after verified-final-diff approval and accepted-main integration. No automation-only conformance or performance claim is permitted. The approved real-client generation and retention requirement remains relocated to P3B, where it can exercise the completed lifecycle rather than being waived or replaced.

## P3 — Transactional lifecycle

Implement clean-state enforcement, isolated migration worktrees, plan/final-diff approval, ownership-aware transformations, drift reconciliation, failure recovery, current/previous-major upgrades, and portfolio-to-site migration. Extend the certification foundation to compiled-CLI add, upgrade, migration, removal, refusal, and recovery journeys. Require the source plan's representative compiled-CLI [add-remove-re-add removal journey and resultant-state assertions](2026-08-04-nextjs-boilerplate-builder-best-reconciled-plan.md#builder-cli-lifecycle-end-to-end-boundary). Perform a one-time coverage backfill for existing executable capabilities: map unchanged valid evidence without repeating expensive checks, and create a separate capability-certification task for every material gap. P3 closure rejects every record that remains `backfill-pending`.

The currently implemented boundaries are read-only `plan-add`, fingerprint-gated `apply-add`, read-only `plan-remove`, and fingerprint-gated `apply-remove` for exact `booking-calendly@0.1.0`, accepted read-only `plan-upgrade` and exact `apply-upgrade` for `standards@0.3.0` to `standards@0.4.0`, and accepted read-only `plan-profile-transition` plus exact fingerprint-gated `apply-profile-transition` for exact `portfolio@0.10.0` to `site@0.10.0`. Application performs only its operation-specific existing-repository transformation, verification and re-inference, migration append, state-last persistence, and exact final-diff inspection before stopping for verified-final-diff approval. Every removal plan requires repository-wide surviving-reference review and exact-path reconciliation of any preserved/ejected capability surfaces before transformation approval; the current planner declares but does not automate those reviews. Accepted `main@7509fc819ba8670040374c350762720848a47ef1` schedules later reference hardening only and is the direct predecessor for `apply-remove`; merged pull request 46 makes the exact executor accepted-main integrated at `main@7f59e8b093edb7be617cd2a30bfb4ebaa6a8ab6e`, and post-merge Repository quality run `32620215344` passed every applicable job. Pull requests 48, 49, and 50 make the standards executor and portfolio-to-site planner/executor accepted-main integrated, with transition execution accepted at `main@641db9537f5dea4911b0b727eb083f8d6d359204`. Merged pull request 51 accepts their private shared control persistence at `main@532a7cd6e874db13ac8c4b1d2f376abe83862772`. Exact `booking-calendly@0.1.0` lifecycle certification and exact `standards@0.4.0` upgrade lifecycle certification are complete. The portfolio-to-site transition lifecycle certification is complete for migration, refusal, exact-state, final-diff, and retained-prefix recovery evidence at exact revision `8098c68c82aaa35a59345706c851e8111d463111`. Exact `content-files@0.4.0` certification is complete at evidence revision `f03b9f624c370728f678924ce34e5287558d2a87`. Exact `section-composition@0.3.0` certification is complete at evidence revision `f74459c8833833186bb651c116ed524e51044677`. Exact `site-routing@0.3.0` certification is complete at evidence revision `77cea944513e521939bf4de088048f67acdfbc3c`. P3 Gate 3 closure is approved and closed against accepted evidence base `main@392f2e27de1d4a24124d51daf059b1667207436e`, whose tree is `0f5b729262237aa6856be4d8e5aa4396584233a2`. GitHub Actions run `32984587387` remained unavailable during the GitHub Actions outage and was explicitly waived for this closure; the waiver is not a passing hosted check. The accepted closure is integrated at `main@e354c4b36a6c1c30bd10b6ac9a7ea42678399fe9`, whose tree is `7645b65a056c643e775987679eeb922e5d5b6ff6`. Repository quality run `33000891104`, attempt 2, passed every applicable job for that exact accepted-main revision. This supplemental hosted evidence closes the residual provider-verification risk without reclassifying waived run `32984587387` as passing. P3 is complete. P3B is the next eligible phase, but this closure does not authorize P3B or P3C implementation. A generic lifecycle executor, another upgrade or profile-transition edge, automated recovery, P3B, and P3C runtime work remain planned or separately gated.

The approved source plan owns the [first supported upgrade planning boundary](2026-08-04-nextjs-boilerplate-builder-best-reconciled-plan.md#first-supported-upgrade-planning-boundary): exact repository-only `standards@0.3.0` to `standards@0.4.0` capability edge planning, its accepted endpoint evidence, compiled-CLI contract, compatibility and refusal rules, no-mutation proof, and executor separation. Merged pull request 47 integrates that read-only boundary at accepted `main@138b5d712ab22016c020eb1c2a3e56e0efc89a5a`; its tree is identical to reviewed planning candidate `cd57c479cb74a5e0f839f7b46ded220bc456b151`, and post-merge Repository quality run `32658708533` passed every applicable job. The accepted boundary provides exact endpoint snapshots, source-version inference, six fingerprinted actions, agreement/refusal enforcement, and temporary linked-worktree no-mutation evidence through builder-core and compiled `plan-upgrade`. Merged pull request 48 integrates exact compiled `apply-upgrade` at accepted `main@af8898b533f4a7ccf08c83bd7818312a5f27c3c0`; reviewed head `3cd51b076d076d39151e96fc1d04c4d91a689a81` and accepted main share tree `a9335c1078d5d613f398196674b6b4a39efab4ab`, and post-merge Repository quality run `32690135067` passed every applicable job. The executor consumes the accepted fingerprint, transforms that one edge once, verifies and freshly infers, appends the migration, persists state last, retains every post-write failure as an inspectable prefix without automatic rollback, proves final agreement and the exact diff, and stops for separate verified-final-diff approval. Accepted `apply-upgrade` does not certify, deploy, publish, approve a generated-project final diff, or establish production readiness. The source plan's [first supported profile-transition planning boundary](2026-08-04-nextjs-boilerplate-builder-best-reconciled-plan.md#first-supported-profile-transition-planning-boundary) owns exact read-only `plan-profile-transition` from `portfolio@0.10.0` to `site@0.10.0`, accepted through pull request 49 at `main@612a963ab96221837b1c8ac815f41e90736d292e`. Its [first supported profile-transition execution boundary](2026-08-04-nextjs-boilerplate-builder-best-reconciled-plan.md#first-supported-profile-transition-execution-boundary) owns the exact fingerprint-gated executor accepted through pull request 50 at `main@641db9537f5dea4911b0b727eb083f8d6d359204`; the [evidence-gated internal lifecycle extraction boundary](2026-08-04-nextjs-boilerplate-builder-best-reconciled-plan.md#evidence-gated-internal-lifecycle-extraction-boundary) owns the current private control-persistence increment. Independently versioned `1.0.0` project/state/migration schemas and builder `0.0.0` remain control context rather than transition subjects.

After the internal extraction and exact Calendly, standards, portfolio-to-site transition, `content-files@0.4.0`, `section-composition@0.3.0`, and `site-routing@0.3.0` certifications are complete, with site-routing evidence bound to `77cea944513e521939bf4de088048f67acdfbc3c`, no capability-certification backfill increment remains. P3 Gate 3 closure is approved and closed.

Under the explicit 2026-08-26 independent-work exception, production `site` implementation proceeded locally from accepted `main@392f2e27de1d4a24124d51daf059b1667207436e` and its tracked site-routing certification receipt while the closure audit completed. P3 Gate 3 subsequently merged through pull request 59 at `main@e354c4b36a6c1c30bd10b6ac9a7ea42678399fe9`; accepted tree `7645b65a056c643e775987679eeb922e5d5b6ff6` exactly matches the reviewed closure candidate, and accepted-main workflow `33000891104` passed every applicable job. Reconciliation rebases the isolated `production-site-profile` branch onto that accepted closure, satisfying the exception's merge gate. The exception never authorized multilingual, analytics, capability certification, deployment, providers, publication, or production action.

Each backfill increment may map unchanged valid accepted evidence, but its currently empty task/evidence record is a subject-binding gap and only its material missing checks may be rerun. P3 Gate 3 closure followed these increments and is approved and closed as an approval gate, not another implementation increment.

**Stop gate:** Transactional failure and recovery fixtures pass and the P3 packet is approved.

## Capability delivery task pair

Every new or materially changed executable capability is delivered through a capability implementation task followed by a separate capability certification task. Implementation approval does not imply certification; the capability and owning phase cannot close, and the capability cannot be advertised as certified, until its certification task passes. The actual admission gate requires a pending record linked to that certification task and binds coverage to the descriptor version or behavior-contract digest; a material change replaces stale active coverage with a new task-linked pending record. The actual phase/release closure rejects records that remain pending. The one-time pre-foundation backfill transition is complete; every current record is either task-linked pending or exact-subject certified. Every capability entry from P3B onward expands into this implementation-task then certification-task sequence before dependent work begins, subject only to an explicitly approved independent-work exception or the [initial authenticated-composition staging](#initial-authenticated-composition-staging) governed by the review protocol.

Each certification task begins with current planning under the [review and contribution protocol](../governance/review-and-contribution.md). The plan supplies step-by-step human prerequisites or explicitly states that none exist, then defines the fresh-scaffold, lifecycle, local runtime, protected-staging, provider, cleanup, and recovery evidence required for that capability. Local runtime evidence remains separate from protected-staging and provider outcomes, and every external action requires separate authorization. No implementation plan or certification task inherits authority to create an account, configure a provider, use a credential, spend money, deploy, or mutate external or persistent state.

## P3B — Client-required public-site expansion

**Production-site increment:** The completed increment advances only `site` to recipe `0.11.0` and `site-routing@0.4.0`. It materializes content-backed home, about, featured-work, not-found, and nested error behavior; active navigation; route metadata; sitemap and robots output; redirects; development and OpenNext/workerd browser contracts; and the exact state-last `site-routing@0.3.0` to `0.4.0` upgrade. The current descriptor subject is certified from accepted local `fresh-scaffold` and `existing-repository-lifecycle` evidence at revision `6034d7330af912d1a1b9bcff3323ed360ebee2d0`; the historical 0.3 receipt remains exact-subject-only. P3 closure is accepted and reconciled. Exact `multilingual@0.1.0` is certified from the same two local evidence kinds at revision `96b587a254cf6fc859867d6fc66c7e0c900c4cfd`. Exact `analytics@0.1.0` implementation is accepted at `main@aa0bcd7e315b11f07e0f4207d11e230ce911b2f4` and its separate certification is accepted at evidence revision `a97341ea628210b6fa713fb12461084f20c3f8da`. The combined representative synthetic client journey is accepted only through the closure record below; production providers or deployment, publication, and production action remain separately gated.

**One-time 2026-08-26 multilingual implementation-lane exception:** Lane B may implement exact `multilingual@0.1.0` for the executable `portfolio` and `site` profiles from accepted production-site predecessor `0a699d26198e94ddfaa596d812ad175284d05c49` on selected base `a66890a6c30a275818e7b51f22ded987cc1d52ff` while Lane A completes separate `site-routing@0.4.0` certification. Lane B owns no Lane A evidence and may merge only after Lane A is accepted, accepted-main verification is green, Lane B is rebased onto that accepted main, and the routing evidence contract is revalidated. This exception does not authorize multilingual certification, analytics implementation, a real client project, provider or deployment action, publication, or a concurrent merge, and it is not precedent for another lane.

**One-time 2026-08-27 analytics implementation-lane exception:** Exact `analytics@0.1.0` implementation may proceed from accepted multilingual implementation `main@2033048c79a777af7e7d2725784bac6e9be3433f` while exact `multilingual@0.1.0` certification proceeds in a separate clean isolated worktree. The multilingual lane alone owns its certification plan, scripts, evidence, registry transition, review, and receipt. The analytics lane must preserve the accepted multilingual descriptor and behavior-contract digest and owns no certification artifact. Multilingual certification merges first; analytics then rebases onto certified accepted main, reconciles, revalidates the multilingual subject, reruns affected verification, and completes an incremental final CodeRabbit review before merging. A descriptor, digest, or certified-behavior change stops for an explicit pending or recertification decision. This one-time order exception is not precedent and permits no concurrent merge; it authorizes no analytics certification, provider mutation, deployment, publication, combined real-client work, or waiver of either final-diff gate.

P3 remains unchanged and is the prerequisite for this boundary. This part relocates approved client-required public-site work so it is delivered through the same transactional capability lifecycle used by every later generated repository:

- finish the production-complete `site` profile, including multi-page routing, navigation, active-link semantics, route-aware metadata, sitemap and robots output, redirects, route-level not-found and error handling, and nested-route production verification;
- deliver `multilingual` as an independently selectable capability during initial scaffolding and addable later through the completed P3 lifecycle, with locale-prefixed routes, strict required translations, locale parity, missing and unused key validation, copy externalization, and localized content paths;
- deliver `analytics` as an independently selectable capability during initial scaffolding and addable later through the completed P3 lifecycle, preserving the analytics/observability boundary and provider-neutral consent with deny-by-default behavior where required, withdrawal handling, and no automatic legal-compliance claim;
- keep Cloudflare Web Analytics, Google Analytics 4, and Microsoft Clarity as independent analytics providers, while Search Console and Looker Studio remain operational integrations rather than runtime analytics providers;
- give each delivered capability a capability implementation task and a separate capability-certification task, including initial-scaffold and fresh-add evidence plus the applicable upgrade, removal, refusal, drift, state, and recovery cases;
- compose a representative synthetic client journey from `site` plus explicitly selected `multilingual` and `analytics`, with the existing independent `booking-calendly` capability selected only when the scenario requires it; and
- ensure the representative synthetic client journey derived from real engagement needs is generated through the public builder path and retained as migration evidence.

The combined journey uses tracked synthetic content and operator-owned non-production accounts under the [shared test deployment boundary](../governance/shared-test-deployment.md). It uses no actual client identity, content, approval, domain, or account and does not establish production deployment, provider certification, French certification, WCAG conformance, legal or privacy compliance, or production readiness.

This boundary creates no composite client profile or capability, hidden defaults, client-specific branch, or duplicate migration path.

The first executable analytics subject is exact `analytics@0.1.0`: hybrid, repository-stateful and external-stateful, reviewed on removal, optional only for current `portfolio` and `site`, and independent of observability. It supports explicit Cloudflare Web Analytics, Google Analytics 4, and Microsoft Clarity runtime selections plus Search Console and Looker Studio operational contracts. [ADR-0012](../adr/0012-purpose-based-analytics-consent.md) and the [canonical executable contract](../architecture/capability-model.md#purpose-based-consent-contract) replace the current provider-group choice with a strict version-2, purpose-keyed local preference bound to the exact notice and configured purpose/provider context for 180 days. Equal allow/deny actions, withdrawal, fail-closed reduction, a safe reload after successful persistence, cross-tab synchronization, and explicit incomplete revocation protect reductions without treating provider identifiers as visitor switches or the preference as an audit receipt.

The descriptor and generated source continue to declare provider domains, Content Security Policy contributions, storage, cookies, purposes, and retention assumptions. Plans redact identifiers, generation and add/remove/re-add remain state-last, and provider accounts and retained data are separate from repository removal. Provider-specific denial or erasure calls and accessible-cookie expiry are bounded effects, not proof of a provider-free document or provider-held data deletion. The architecture change adds no dependency, backend, provider mutation, certification, compliance claim, or observability coupling.

The implementation creates no public package and no generic lifecycle executor. A generic retained `site` plus multilingual plus analytics fixture provides deterministic generated unit, component, Next/OpenNext build, development-browser, and workerd-browser evidence with provider requests locally intercepted. The implementation itself performs no provider provisioning or mutation, no deployment, and no legal-compliance claim; it does not use client data or credentials, spend money, certify analytics, or establish production readiness. The separate analytics certification successor is complete from accepted `cleanup-recovery`, `deployed-application`, `existing-repository-lifecycle`, `fresh-scaffold`, and `provider-confirmed` outcomes and does not broaden the implementation or production authority.

### Relocation ledger

No approved task, requirement, gate, or identifier is removed:

- P2 retains every approved portfolio task and stop gate; only the representative synthetic client-journey generation and retention requirement moves here so it can exercise P3.
- P3 retains every approved lifecycle, migration, removal, refusal, recovery, state, certification, and approval requirement unchanged.
- P4 production-complete `site` work moves here as the `site` implementation and certification pair.
- P5A multilingual work moves here intact; its capability identifier and requirements remain unchanged.
- P5B analytics and provider-neutral consent work moves here intact; its capability identifier and requirements remain unchanged.
- P4 app-foundation, `app`, and portfolio/site-to-app transition work remains in P4.
- P5C through P5F retain their P5 identifiers and requirements; the later [contact deferral](#contact-and-portability-work-deferred) removes P5F from active scheduling.

P5A and P5B are therefore not deleted and not renumbered.

**First client-ready milestone:** The representative synthetic client journey is generated and retained only after the production `site` and each explicitly selected capability pass their P3B gates.

**Stop gate:** Approve the production `site`, independent multilingual and analytics capability evidence, the combined representative synthetic client journey, and the retained migration fixture before app-foundation work begins.

**Closure status:** P3B closure is approved and closed against accepted evidence base `main@f7caa6ef0b8103c051c99eb6599debf969b8489c`, whose tree is `87d8be28fa2d611a07b5eb7c32712f0245d12b87`. Exact `site-routing@0.4.0` remains certified at evidence revision `6034d7330af912d1a1b9bcff3323ed360ebee2d0`, exact `multilingual@0.1.0` at `96b587a254cf6fc859867d6fc66c7e0c900c4cfd`, and exact `analytics@0.1.0` at `a97341ea628210b6fa713fb12461084f20c3f8da`. The combined journey implementation merged at `9cd386ee637cad85162917b4f4a91e6c878fb75e`, its hosted redirect repair merged at `e116adbff366be2bf674846f231e26a6e00d132f`, synthetic journey run `33456304959` passed at that repaired revision, and compatibility recovery run `33456850947` passed immediately afterward. The retained migration repository preserves pristine commit `d0ad7eaaa9c7198a8a479540bdd8b4dcf4dee113` as an ancestor of clean adapted `main@3583e88902c8b2328da90b77e0a4edd112cdfd23`. Repository quality run `33588604465` for exact accepted `main@f7caa6ef0b8103c051c99eb6599debf969b8489c` passed every applicable job, including the generated visual-regression boundary. This synthetic evidence does not establish production readiness, provider availability, visual or linguistic quality, legal or privacy compliance, human accessibility, or WCAG conformance. P3C is the next eligible phase, but this closure does not authorize P3C planning or implementation, app-foundation work, deployment, publication, provider mutation, or any production action.

## P3C — Automated removal-reference hardening

P3C began after P3B closed and did not reopen or weaken P3 or P3B. P3C strengthens the accepted manual, fingerprint-bound removal-review contract with bounded Git-identity inventory and capability-specific automated reference guards. Exact resolved conflicts refuse or require explicit reconciliation; heuristic and incomplete-coverage findings require review; finding no detected match must never be represented as proof of dependency absence or complete removal. The accepted executor recomputes the guarded plan before its first write. Provider resources, credentials, and persistent data remain separate disposition and recovery domains. Generic extraction remains evidence-gated.

**Calendly guard integration status (2026-09-02):** Pull request 92 integrates the first bounded guard for exact `booking-calendly@0.1.0` at `main@4f078a6a28b841df7bb96df477a1e99b07bed613`; its pull-request checks passed. Post-merge Repository quality run `33662217111` passed builder/packages, generated-fixture determinism, capability certification, compatibility proof, lint, and typecheck, but failed the generated visual-regression step with `DEPENDENCY_AUDIT_FAILED`. Pull request 93 integrates the focused generated dependency-audit repair at `main@5800bc21407703ec2c935cd1feb3fa3c669ec467`; its pull-request checks passed, and post-merge Repository quality run `33695662200` passed every applicable job for that exact accepted-main revision. The guard and its accepted-main dependency-audit repair are integrated with a green post-merge quality result.

**Additional capability-wide increment — integrated:** Pull request 95 extends the same bounded repository-reference protection to exact `multilingual@0.1.0` and `analytics@0.1.0` while preserving the accepted Calendly behavior. The [approved source plan](2026-08-04-nextjs-boilerplate-builder-best-reconciled-plan.md#additional-capability-wide-repository-reference-increment) owns the exact test-driven implementation boundary. The accepted implementation shares only the proven inventory, projected-tree analysis, exact deleted-path resolution, path-only warning, and pre-write revalidation mechanics in private capability-neutral code. Capability-specific heuristic tokens and removal reviews remain finite and explicit. All three current removable capabilities declare no removable packages, so package-backed hardening remains deferred until a concrete removable package exists. Repository deletion does not perform or prove provider-resource, credential, browser-storage, cookie, retained-data, deployment, certification, or production action.

**Entry and direct predecessor:** Post-repair run `33695662200` passed every applicable job for exact `main@5800bc21407703ec2c935cd1feb3fa3c669ec467`, satisfying the predecessor quality condition used by the accepted implementation. Its review evidence records the exact accepted reconciliation predecessor and ancestry. A failed or pending result remains ineligible to be relabeled as passing.

**Stop gate:** The bounded Calendly guard remains behaviorally unchanged, and exact multilingual and analytics surviving references refuse with sorted repository-relative path-only output. Heuristic, dynamic-loading, binary, link, unreadable, oversized, and truncated coverage remains fingerprint-bound review evidence; every guarded capability is rechecked immediately before the first write; refusal performs no repository, state, migration, provider, or persistent-data mutation; and existing provider-disposition and preserved/ejected-surface reviews remain mandatory. Before app-foundation work begins, deterministic focused tests, privacy-safe output, identity-change refusal, executor-revalidation, no-mutation evidence, the complete builder-kernel candidate, and independent requirements, architecture and anti-overengineering, and test-evidence reviews must pass. Any then-concrete package-backed guard and any reusable-analysis claim retain their separately named evidence gates.

**Closure status (2026-09-04):** Pull request 95 reviewed candidate `5d1e22a64c4d1b6dce476fa6a8ce02134e0fb6fa` with candidate tree `c66ba69d1206d3d3c66814f06105ebd0f3c73461` and accepted merge `51c1d2e48220e403fad68e9ce0f790df9e56cb6e` with the same tree. At closure, the accepted merge was the refreshed current accepted main, so no later accepted-main change superseded or conflicted with the candidate. Post-merge Repository quality run `33757605858` completed successfully for that exact accepted merge: jobs `scope`, `builder-and-packages`, and `generated-projects` succeeded; `compatibility-proof` was skipped because no compatibility-proof scope path changed; and `dependency-review` was skipped because the run event was `push` and the job is pull-request-only. The skipped jobs are inapplicable by workflow contract, not passing evidence. The stop gate is satisfied for the three exact current removable capabilities while package-backed analysis remains deferred until a concrete removable package exists. This documentation reconciliation adds no runtime, provider, certification, deployment, publication, or production authority. P3C is complete. P4 is the next eligible phase, but this closure does not authorize P4 planning or implementation. The separately approved clean-restart plan authorizes only the architecture and governance candidate described below.

## P4 — App foundation

The current generation boundary implements internal hybrid `app-foundation@0.1.0` and `app@0.2.0`, whose exact recipe is `app-foundation@0.1.0 + site-routing@0.4.0`. App project/state parsing, deterministic rendering, and state-last new-directory generation retain the complete production-site experience and independently support every subset of Calendly, multilingual, and analytics without automatically adding stateful backend infrastructure.

The generated app pins `effect@4.0.0-rc.112` directly and confines it to application, infrastructure, composition, and delivery server modules. Request context stays explicit; an application-owned build-information service receives Cloudflare or in-memory Layers; delivery runs one lazy program once and interprets defect before interruption, interruption before recognized typed failure, and unrecognized or mixed residual failure as unexpected. Pure interruption uses the native abort, `deployment-cloudflare` alone owns `enable_request_signal`, cancellation claims exclude already-started provider work, and the initial application has no retry.

Read-only planning and fingerprint-gated execution retain the original app edges and add only the same-generation Vitest 5 edges defined by [ADR-0015](../adr/0015-vitest-five-generation.md) and the [capability model](../architecture/capability-model.md#accepted-app-architecture-boundary). Each must cover all eight optional-capability subsets, preserve compatible content and UI, perform no deletion, replace the builder-kernel-owned root lockfile exactly, keep `.egeria` persistence state-last, revalidate all four visual cases, and refuse unproved changes before mutation. At implementation acceptance, app-foundation plus the nine widened existing descriptor subjects remained pending certification under fresh evidence; the accepted local outcomes are recorded below.

**Accepted predecessor:** Increment 4 activates exact app addition/removal for Calendly, multilingual, and analytics through the existing planners and state-last executors. It preserves the accepted Effect runtime, foundation/routing subjects, dependencies, templates, and retained portfolio/site fixtures. App execution requires the existing whole-Worker verification receipt. Ordinary reference and ownership preconditions continue to apply; the [capability model](../architecture/capability-model.md#accepted-app-architecture-boundary) records the conservative analytics/multilingual removal constraint. Actual generated-app transactions, refusal/recovery tests, and composition comparisons supply the candidate evidence.

**Accepted planning predecessor:** Increment 5 merged through pull request 110 at `main@6ccc6a7f0ced16cf9bc712630a03951ba4b48b3f`, with successful post-merge Repository quality run `34579273392`. Its content-preserving planner and separate eight-subset visual producer remain the policy and visual owners.

**Accepted execution predecessor:** Increment 6 merged through pull request 116 at `main@4a86ff9aeccb216170e0a6c79806df12ad66fd06`, with tree `66b4b2d4572a1db10c660a15e8c801cf8116b26e`. Post-merge Repository quality run `34617150457`, attempt 1, completed successfully for that exact revision. Dependency review was inapplicable to the push event, and compatibility proof was skipped by the existing path classifier; neither skip is passing evidence. The accepted executor and unchanged atomic writer retain the [capability model](../architecture/capability-model.md#incoming-app-transition-execution) as the owner of complete pre-write revalidation, content preservation, app verification, project/migration/state ordering, dynamic final-diff checks, and retained-prefix recovery.

**Accepted CLI predecessor:** Increment 7 merged through pull request 117 at `main@f82472bce26fc0752133113ca18472bd490444bd`, with the same tree `ec1682c7059b005c7ce0cfaf8ec4842aa83498c1` as final PR head `a4d5237d1b363471392a713f903f06c8a4b27dee`. Post-merge Repository quality run `34652963916`, attempt 1, completed successfully for that exact revision. Dependency review was inapplicable to the push event, and compatibility proof was excluded by the unchanged path classifier. The CLI delegates app creation and the three supported profile-transition edges to the existing builder-core owners.

**Accepted fixture increment:** Increment 8 added exactly `fixtures/generated/app` and `fixtures/generated/app-all-optional-integrations` through compiled CLI production generation. The fixed-root verifier retains immutable inputs, exact dependency policy, owned temporary execution, separate executed/skipped whole-Worker receipts, and shared Effect source/build inspection. At that accepted fixture boundary, the five earlier fixtures, eight generation subsets, sixteen incoming-transition cases, dependency owners, and visual matrix remained unchanged. Exact generation provenance, byte identities, command results, and review dispositions belong to the ignored fixture review packet under the [capability model](../architecture/capability-model.md#representative-immutable-app-fixtures).

**Accepted fixture predecessor:** Pull request 118 merged at `f4d4833e33893719c36f5848f22e1749fc659488`, with tree `8d8126529c72f0df7ea096bcc554bb1bf8557262` identical to its final reviewed candidate. Post-merge Repository quality run `34731575539` passed builder-and-packages, scope, generated-projects, and app-transition-visuals; dependency review and compatibility proof were inapplicable under their existing rules. This exact accepted predecessor is the Vitest migration baseline.

**Accepted migration:** Pull request 119 merged the Vitest 5 increment at `e18cac6c7e0d47c072050bba8468ddf2d48003b9`, selecting standards `0.5.0`, portfolio `0.11.0`, site `0.12.0`, and app `0.2.0` under ADR-0015. It preserves retained descriptors, output, locks, diagnostics, and supported operations; adds equivalent same-generation incoming app edges; and regenerates the seven current fixture roots. At migration acceptance the new standards subject remained pending and received no historical certification evidence; its separately accepted current outcome is recorded below.

**Accepted implementation closeout:** Increment 9 merged through pull request 120 at `4da1ad1e48c12567d4e477c4ecbc3b9d4e097922`, preserving the reviewed three-file documentation patch. Post-merge Repository quality run `34770558947` completed successfully for that exact revision: scope, builder-and-packages, generated-projects, and app-transition-visuals succeeded. Dependency review was inapplicable to the push event, and compatibility proof was excluded by the existing path classifier; neither skip is passing evidence. This closes the implementation sequence, not capability certification or P4.

**Current certification work:** The coordinated renewal records six accepted local subjects at evidence revision `fe16ae41153f89242cdf48c0f899c3720f511557`: app-foundation, standards, content-files, section-composition, site-routing and multilingual. Their fresh generation, applicable compiled lifecycle, whole-Worker, Effect build and visual evidence passed, and the user accepted the subject-specific outcomes and documentation/recovery limits. Post-merge Repository quality run `34799203126` passed every applicable job at that revision. The user accepted exact default-app deployment and observability fresh-scaffold, deployed-application and cleanup-recovery outcomes at evidence revision `9ea5dce328a9a065f71e168cb2c82ea515f54ae1`: diagnostics run `34926395956` and recovery run `34927988761` passed, provider readback reconciled, and the shared Worker returned to its verified compatibility baseline. The unchanged observability admission record is certified. The accepted default deployment `0.3.0` receipt remains bound to its retained subject; it does not certify persistence-specific deployment `0.4.0`. Analytics and booking-calendly remain pending for the default baseline. After PR #129, the [certification registry](../../certifications/capabilities.json) contains eleven admission subjects, with six certified and five pending; changed standards and deployment plus new persistence retain their separate renewal plans. [Certification coverage](../architecture/capability-model.md#certification-coverage) distinguishes current admission from the retained default baseline. P4 remains open; certification of already-merged persistence follows the [certification-only sequencing amendment](#one-time-2026-09-17-persistence-certification-sequencing-amendment). App capability upgrades remain unsupported. Certification, merge, deployment, provider mutation, publication and production action retain their separate gates.

**Stop gate:** Complete every subject's required evidence and review, the all-certified machine closure check, and explicit phase acceptance before closing P4. The separately admitted [environment stream](#lean-application-environments) does not close or waive this gate. The [certification-only sequencing amendment](#one-time-2026-09-17-persistence-certification-sequencing-amendment) controls the limited reconciliation for already-merged P5C; it does not open another product increment. The [Queue topology proof exception](#one-time-2026-09-23-queue-topology-proof-exception) permits only test-only P5E-1 before closure; the subsequent [background job runtime exception](#one-time-2026-09-23-background-job-runtime-exception) separately permits only the P5E-2 candidate through an open MR. The separately user-directed [hosted contact form pair](#hosted-contact-form--next-two-increments) permits only its two named increments before this phase closes. The [one-time parallel implementation exception](#one-time-2026-09-14-persistence-implementation-exception) records the earlier isolated development authority. Local execution, implementation closeout, historical certification, or passing image comparisons alone cannot satisfy the closure gate. New visual inputs still require separate exact-manifest human approval, and baseline promotion requires its own authorization. The [capability model](../architecture/capability-model.md#accepted-app-architecture-boundary) owns verification and claim boundaries.

## Hosted contact form — next two increments

**Integrated delivery status (2026-09-23):** Resend's contract, generation and existing-repository lifecycle were integrated together in [PR #138](https://github.com/Egeria-Systems/egeria-scaffold/pull/138) at `18783ceeeef96d78793936159e1d88745c2c46ab`; [ADR-0017](../adr/0017-transactional-email-resend.md) owns the accepted decisions. This delivery covers the original P5D-1–P5D-3 implementation breakdown; those historical cards are not new assignments. Web3Forms implementation followed in [PR #139](https://github.com/Egeria-Systems/egeria-scaffold/pull/139) at `117f4c8a5d8563923d68efe2738fa57babfb7dfd` under [ADR-0018](../adr/0018-hosted-contact-form.md). Web3Forms certification subsequently integrated through [PR #144](https://github.com/Egeria-Systems/egeria-scaffold/pull/144), with its accepted exact receipt preserved. Merged PR #145 supplies accepted local P5E-1 evidence with its stated limitations. MR #146 subsequently merged P5E-2 at `fb16c9128d8e461f52a8ad6a2bb4b1e7a368d566`; the user explicitly accepts its candidate `086540a433d4dd7e72a79d0b694e3f0cc1637dbc` and documented limits as the next implementation predecessor. The [lifecycle/operator exception](#one-time-2026-09-24-job-lifecycle-and-operator-safety-exception) prospectively opens only P5E-3 through an open MR; Resend certification remains required separate later work. The [certification registry](../../certifications/capabilities.json) owns their current status and task handoffs. The merges do not certify either provider or establish P4 closure, and the historical integration requirements below remain separate reconciliation obligations.

**User-directed sequencing amendment (2026-09-22):** The immediate next increment implements `contact-form-web3forms`. Its direct successor certifies **Web3Forms only**. No additional Resend implementation, integration or certification task is inserted before or between this pair. Both Web3Forms and Resend certification are required. Web3Forms receives immediate certification because the user will use it soon. Resend certification remains a required separate later increment with its own prerequisites, full quality gates and evidence; it does not require immediate scheduling after its implementation. P5C–P5F keep their identifiers and requirements.

This bounded exception allows the pair to proceed through ordinary implementation, integration and certification gates before formal P4 closure or Resend acceptance. It changes scheduling only: the sequence decision is not approval of either exact-file execution plan, final diff, merge or external operation. P4 remains open and its complete `all-certified` closure requirement is unchanged.

**Implementation predecessor and isolation:** Use the accepted local foundation predecessor `fe16ae41153f89242cdf48c0f899c3720f511557`, whose app-foundation acceptance receipt is named `2026-09-13-app-foundation-app-certification-receipt.md` and whose accepted evidence is recorded in [P4](#p4--app-foundation). The selected source base is its descendant `18783ceeeef96d78793936159e1d88745c2c46ab`, confirmed by a fresh main fetch on 2026-09-23 with Resend implementation integrated. At the user's request, the `contact-form-web3forms` branch was created at that exact revision in a dedicated worktree; the reviewed roadmap delta was reconciled there while preserving the original planning checkout based on `63a17e9`. Integration observation does not establish P4 closure, certification or successful post-merge checks. At Gate 1, revalidate acceptance, ancestry and applicable checks, and freeze the exact comparison for this prepared branch. Preserve the P4 and Resend worktrees, receipts, resources and current subject statuses. Implement only the hosted contact capability and direct consumers; serialize changes to shared owners and reconcile against accepted main before integration. Web3Forms implementation must now prove local coexistence with integrated Resend, preserve its retained foundation and all nine fixtures, and preserve the reconciled roadmap amendments and existing email sequencing section. Live Resend certification remains separately required later.

**Certification predecessor and scope:** The directly following increment starts from the accepted, integrated Web3Forms implementation and its exact acceptance packet, revision, descriptor digest and applicable successful hosted checks. It proves fresh generation, existing-repository lifecycle, deployed browser submission, provider confirmation with an independently observed synthetic mailbox receipt, and cleanup/recovery. Resend sending, Resend credentials and renewal of the email candidate's foundation subject belong to the separate Resend task. Web3Forms acceptance neither certifies Resend nor closes P4.

**Quality and stop gates for each increment:** Apply the full [review and contribution protocol](../governance/review-and-contribution.md): Gate 1 preparation, separately approved Gate 2 exact-file plan, test-driven changes and proportionate full verification, the approved bounded read-only Ponytail review, independent requirements, architecture and anti-overengineering, and test-evidence reviews, disposition of material findings, and a review packet followed by Gate 3 verified-final-diff approval. Integration retains applicable hosted checks and explicit authority. Certification additionally requires its current human-prerequisite runbook, separately authorized deployment/provider/message/cleanup actions, exact-subject evidence and explicit acceptance of actual outcomes. Pending unrelated subjects remain pending; the global closure check must continue to reject them. The three design decisions were pending at the original sequencing approval; the implementation-specific authorization below records their subsequent acceptance.


**Implementation-specific authorization (2026-09-23):** The user approved the reviewed exact-file implementation plan and its three choices: current portfolio/site/app recipes, direct browser fetch with on-page localized outcomes, and free hCaptcha with provider-side enforcement reserved for the immediately subsequent certification increment. For this implementation only, explicit Gate 2 approval and a prospective waiver of separate procedural Gate 2/Gate 3 pauses authorize bounded implementation, controlled verification, required independent reviews, validated material repairs, focused commits, branch push and one open non-draft MR targeting main with applicable successful non-deploying hosted checks. The user explicitly invoked bounded `@ponytail-review` of the capability and direct consumers and separately authorized validated material behavior-preserving repairs. This does not claim personal inspection of an unseen final diff: final human review occurs on the open MR. The global review protocol, substantive evidence gates, fixture/receipt preservation and certification sequence remain unchanged. Merge, deployment, live credentials/provider actions, real submissions/email, spending, provider-data deletion, live certification, visual-baseline changes, external review-service submission and review-comment replies remain outside this authorization.


**Approved baseline fixture exception (2026-09-23):** After preparation found pre-existing Resend renderer/fixture drift, the user approved targeted regeneration of `portfolio-email` and `app-persistence-email` adapter files and recorded fingerprints to preserve merged additive success-response handling. All other existing fixture bytes, merged runtime behavior and historical receipts remain protected. The actual baseline has ten retained fixture roots; this exception changes only the two identified fixture subjects and does not weaken verification. The new contact fixture uses the current site recipe to declare its already effective patched Next.js version directly; temporary portfolio coverage and all historical fixtures remain preserved.

## Lean application environments

**Admission recorded 2026-09-24:** The user directly admits one ten-card environment stream from exact baseline `4b713dabbbf0b3c405c0fa07352e90e6de629d3d`. This is new bounded admission, not retrospective acceptance of all merged work. [ADR-0020](../adr/0020-application-environments.md) owns the architecture and precise supersessions; the [governance exception](../governance/review-and-contribution.md#lean-application-environments-delivery-exception) owns delegated approvals, serialized work/publication and reviewed published unmerged predecessors.

**Evidence:** The accepted foundation receipt `docs/implementation-evidence/2026-09-13-app-foundation-app-certification-receipt.md`, recorded under [certification coverage](../architecture/capability-model.md#certification-coverage), binds its exact `0.1.0` subject to ancestor `fe16ae41153f89242cdf48c0f899c3720f511557`, not the wider admission subject below. Existing observability and hosted-contact receipts remain exact-subject evidence. [Repository quality run 36015266198](https://github.com/Egeria-Systems/egeria-scaffold/actions/runs/36015266198) passed every applicable job at this baseline: builder-and-packages, scope, app-transition-visuals, generated-projects and compatibility-proof; dependency-review was inapplicable to the main push. Verification is not human acceptance or certification. The bounded audit did not establish later email integration acceptance or a final jobs acceptance artifact; preserved private preparation/review material does not fill those gaps. This direct admission permits only this stream's use of the actual baseline and named pending subjects. P4 remains open, and unrelated downstream admission remains gated.

**Frozen subject inventory:** The following snapshot is from the [registry](../../certifications/capabilities.json) at the bound revision. It identifies six certified and eight pending subjects, including the exact pending shared inputs admitted solely for this assembly. It does not reset records, transfer certificates or permanently fix current versions. The registry remains the machine owner; future cards compute actual version/digest/evidence deltas from their real input trees.

| Capability | Version | Behavior-contract digest | Status | Causal renewal assessment |
| --- | --- | --- | --- | --- |
| `analytics` | `0.1.0` | `sha256:6c562317c6888a0c4a1b14bb2d7320f309b7c6ac3927a4b94cb3e9365ae01bba` | pending | Settings, consent, build activation and provider isolation |
| `app-foundation` | `0.2.0` | `sha256:16eb4a047176fead1fbada2f8b6b0ca3cda8e7faa57ca228763ee3392c14d47a` | pending | Compiled/runtime target and current-profile composition; assess installed default 0.1.0 separately |
| `application-persistence` | `0.1.0` | `sha256:dcb911f024f4a0cf792396bf919e2387f0193fae6608071c6dea7aa371eb4c95` | pending | Target mapping, bindings, preflight and workflow evidence |
| `background-job-delivery` | `0.1.0` | `sha256:97c9aa94d8c489ec6eaca1a61258546aa511105c5f3a565509da01e752f0883b` | pending | Target/Worker/deployment inputs; lifecycle remains unsupported and pending |
| `booking-calendly` | `0.1.0` | `sha256:f9ee03e776da520af1bef7079a12454fd5339205f04d9836a424d5011da1bdca` | pending | Target input, local absence and mode/composition evidence |
| `contact-form-web3forms` | `0.1.0` | `sha256:e462b6432eaac905b973d2122c122abca5fdd498e262ee63f7f2508b16978be9` | certified | Target input and unavailable/deployment behavior; preserve old receipt |
| `content-files` | `0.4.0` | `sha256:0e6519573a119a1e09b90421189c55ec81422382c8bd10429f977e1e129029c4` | certified | Only actual changed content/metadata/shared evidence inputs |
| `deployment-cloudflare` | `0.6.0` | `sha256:55c727c3636b4405e489d6e65d5b435228f50b1ef6ede51293d44556137576fd` | pending | Target/artifact/credentials and all affected installed 0.3.0–0.6.0 variants |
| `multilingual` | `0.1.0` | `sha256:48a3ac0f39e8f356fbc9bc63b95f2d4fb7d334aee8cbef0726800ee10fbd9891` | certified | Only changed localized copy and composed evidence |
| `observability` | `0.3.0` | `sha256:0fa9530d9b2b6de0438cadd400a80909e8f55a5cb6c3d7b3ecc59724088c5f43` | certified | Exact staging credential exception, destination and privacy evidence |
| `section-composition` | `0.3.0` | `sha256:9f9830fd6fc0674d3851e42ca6b7f3a14b2182b60a994bc470399043b98b771e` | certified | Only changed rendered UI or shared verification inputs |
| `site-routing` | `0.4.0` | `sha256:a8bd53e9b32546266efd3dde9dc96fc3914cb06e9e811b8bf96ebd42822e2dac` | certified | Origin/noindex/metadata and genuinely affected transitions |
| `standards` | `0.6.0` | `sha256:631efaeae569c27f225e9df3eeaacd28537f66a30f1533da70185e017508a4fd` | pending | Common configuration, generated checks/examples and workflow inputs |
| `transactional-email-resend` | `0.1.0` | `sha256:558ff80dacef968fecf3e8a34e52ecd6672f0619a8fee1b73dcbb9d094a04ff3` | pending | Target, exact recipients, sender/domain and outcome evidence |

Changed source, locks, workflow or runner inputs can require renewal even without a changed descriptor digest. Each card records old/new subjects, changed required evidence, still-applicable causal evidence, exact pending task and missing local/external/human outcomes. Required-evidence changes travel with coherent implementation. Separate certification retains its own plan, authorization and accepted outcomes. Historical receipts survive retirement of related unused generation support.

### Environment delivery checkpoints

| Card | Coherent implementation and documentation owner |
| --- | --- |
| 1. Accept architecture, entry and future-service requirements | This documentary admission, ADR, canonical references and all future card/certification amendments; no runtime or registry change |
| 2. Establish common configuration and internal generation | [Common internal candidate](../architecture/capability-model.md#common-configuration-internal-candidate) implements the finite version/schema ledger, shared target helper, selection-only parsing, state-last generation/inference/doctor, foundation guard and shared guide/examples. Candidate verification and independent acceptance remain separate; public activation and provider consumers remain later checkpoints. |
| 3. Complete hosted contact configuration | [Internal contact candidate](../architecture/capability-model.md#hosted-contact-internal-candidate) adds exact contact `0.2.0`, selected public build input, localized absence, contact-only guarded lifecycle and operator examples. Controlled browser/artifact and compiled lifecycle evidence remain distinct from acceptance, provider certification and later public activation. |
| 4. Complete booking configuration | One target for all modes, local absence and isolated test calendar guide/example |
| 5. Complete analytics, consent and analytics-related guidance | All providers, exact build flag, consent context, Search Console/Looker and provider-specific guide/examples |
| 6. Reconcile persistence environment selection | Existing target mapping, D1 safeguards, controlled binding evidence and guide/example |
| 7. Complete transactional email safeguards | Exact recipients, target/sender/domain, bounded outcomes and Resend guide/example |
| 8. Complete observability configuration and browser diagnostics | Optional provider configuration, exact staging cookie exception and diagnostic guide/example |
| 9. Complete deployment, artifact verification and site URLs | All four deployment variants, separate prepared artifacts, Access transport, native approval contract, metadata/noindex and jobs target/retention/attestation guide/examples |
| 10. Release the complete generation and reconcile evidence | Verify coherent public CLI/recipes/lifecycle/guides and representative composed fixtures; activate complete local candidate atomically and reconcile exact subject/certification handoffs |

Every card requires its exact plan, evidence-gap or meaningful RED/GREEN checks, bounded review, verified-diff checkpoint and separately granted publication window. The [capability boundary](../architecture/capability-model.md#application-environments) owns current public and stateful composed acceptance; their consumers ship examples and tests in the owning cards above. Documentation is not deferred to the final card. No current business contact-notification endpoint, queue-to-email workflow, replay tool or future provider is introduced for an example.

During internal assembly public defaults remain byte-identical to the last complete generation. The final checkpoint activates only a coherent generation and retires related unused support under ADR-0020; it does not erase historical evidence or create migration paths for nonexistent clients. “Complete and releasable” additionally requires all required release certification and acceptance outcomes. Source activation does not authorize package publication, live certification, deployment or P4 closure. Stop at each scoped approval gate and before any separately gated operation.

The [future environment acceptance clauses](2026-09-16-remaining-program-delivery-plan.md#application-environment-acceptance), all 28 delivery cards and every certification row own later service, guide, composed-flow and recovery obligations. Existing detailed private persistence/email/jobs handoffs remain exact historical-subject inputs and must be rebound before later certification; they cannot carry contradictory old environment assumptions into active work. The remaining decisions preserve D1–D12, all dependencies, policy gates and phase statuses.

## P5 — Remaining independent backend capabilities

The [owning delivery cards and certification rows](2026-09-16-remaining-program-delivery-plan.md#application-environment-acceptance) include environment configuration, composed guide/examples, negative cases and recovery at first executable delivery; existing prerequisites and phase closure remain.

The [remaining-program delivery plan](2026-09-16-remaining-program-delivery-plan.md) decomposes P5D through P10 into outcome increments, dependency and requirement coverage, and separate certification tasks. Its [historical email contract handoff](2026-09-16-email-delivery-contract-plan.md) preserves the original planning record; [recorded decisions](2026-09-16-remaining-program-decisions.md) explain the approved directions. Current sequencing follows the [integrated delivery status](#hosted-contact-form--next-two-increments). These plans do not change current phase acceptance or authorize runtime work.

P5A and P5B are relocated intact to P3B; they are not deleted and not renumbered. P5C through P5F retain their identifiers and requirements here, subject to the [contact deferral](#contact-and-portability-work-deferred):

- P5C: `application-persistence`
- P5D: `transactional-email-resend`
- P5E: `background-job-delivery`
- P5F: `durable-contact-submissions` — deferred, including its separate contact certification tasks.

P5C through P5E may be developed independently after P4 when they do not alter shared contracts concurrently, but merge sequentially with fixtures rerun after each merge. The bounded persistence, transactional-email, [Queue topology proof](#one-time-2026-09-23-queue-topology-proof-exception) and [background job runtime](#one-time-2026-09-23-background-job-runtime-exception) exceptions below permit only their named isolated work before P4 closure. If separately reactivated, P5F retains its P5C prerequisite; its email and queue integrations require P5D and P5E respectively.

### One-time 2026-09-24 job lifecycle and operator safety exception

The user explicitly accepts merged P5E-2 MR #146: candidate `086540a433d4dd7e72a79d0b694e3f0cc1637dbc`, merged revision `fb16c9128d8e461f52a8ad6a2bb4b1e7a368d566`. This is the direct implementation predecessor with its documented limits, including acknowledged-message redelivery in the pinned emulator. Merged P5E-1 evidence remains MR #145 at `335ddb5a2eb6269b2fe5b72db04554d1c1f86972`. The retained foundation predecessor remains `app-foundation@0.1.0` at `fe16ae41153f89242cdf48c0f899c3720f511557`, accepted by `docs/implementation-evidence/2026-09-13-app-foundation-app-certification-receipt.md`; current foundation `0.2.0` is a distinct pending subject.

This new prospective exception permits only isolated P5E-3 existing-repository jobs lifecycle and bounded operator inspection/replay/drain machinery through one open non-draft MR before formal P4 closure. The freshly fetched selected base is `44741ff6b78a68cfd85e15285a370fe40f6117d9`, on branch and managed worktree `job-lifecycle-operator-safety`. Private preparation binds the exact path, ancestry, frozen comparison and Repository quality run `36025269518`. Repository quality run `36025269518` completed successfully for this exact base: scope, builder-and-packages, generated-projects, app-transition-visuals and compatibility-proof passed. Dependency-review was inapplicable/skipped on push and is not passing evidence. The baseline is accepted for this bounded implementation. Subsequent main dependency updates remain intact and the actual current pinned graph must be tested.

Preserve ADR-0019's shared primary OpenNext Worker, cohesive separate HTTP and Queue modules, independent jobs selection on current portfolio/site/app, exact defaults and retained installed tuples, existing handler contracts and 24-hour / 86,400-second primary and dead-letter retention. No second topology, implicit persistence/email, paid retention or archive is authorized. Scope is the named lifecycle/operator candidate; unsupported operations continue to refuse. Source removal requires reviewed current work disposition and never implies remote drain or resource deletion. Source, deployed consumer and queued-message recovery remain distinct; source rollback cannot undo side effects or recover expired messages.

Protect immutable fixtures, historical receipts, unaffected certification records, other worktrees, provider resources and leases. Renew only new or materially changed subjects, including changed causal evidence beyond descriptor hashes, with task-linked pending records and no carried-forward certification. Current standards, deployment, foundation, persistence, email and jobs subjects retain independent certification obligations; admission does not establish P5E-C or global closure. Preserve earlier sequencing and acceptance discrepancies rather than retrospectively declaring their prerequisites satisfied.

The subsequent explicit user decision approves targeted DLQ-copy deletion only after confirmed replay acceptance and a durable checkpoint, with uncertainty requiring reconciliation and drain observing the existing consumer. Retain the actual jobs `0.1.0` generation contract unchanged; new generation/addition selects `0.2.0`, removal supports either version and re-addition selects `0.2.0`. In-place jobs upgrades remain unsupported. This changes neither the existing handler/message contract nor provider-action authority.

For this delivery only, procedural Gate 2/3 pauses are prospectively waived. Exact preparation and file plans, TDD, frozen comparisons, bounded `@ponytail-review`, independent requirements, architecture/security/anti-overengineering and test-evidence reviews, validated material repairs, focused commits, push, applicable non-deploying hosted checks, substantive configured CodeRabbit review and a final private review packet remain required. Material unresolved decisions still need the user's answer. This authority does not claim inspection of an unseen diff or authorize merge, deployment, live Queue/credential/provider operations, spending, persistent-data changes, P5E-C, P5F or P4 closure. Later integration requires separate human authority plus reconciliation of current main/ancestry, P4 closure and historical discrepancies, affected dependency/certification subjects, verification and applicable hosted checks.

### One-time 2026-09-23 background job runtime exception

The user explicitly authorized isolated P5E-2 fresh-generation implementation, verification, independent review, validated material repairs, focused commits, branch push and one open non-draft MR targeting main before formal P4 closure. The accepted retained predecessor is `app-foundation@0.1.0` at `fe16ae41153f89242cdf48c0f899c3720f511557`, bound to `docs/implementation-evidence/2026-09-13-app-foundation-app-certification-receipt.md`. Current `app-foundation@0.2.0` remains a distinct pending subject. Merged P5E-1 evidence is PR #145 at `335ddb5a2eb6269b2fe5b72db04554d1c1f86972`, also the freshly fetched exact main base for branch and managed worktree `background-job-delivery`. Private preparation records bind the exact local path, ancestry, applicable hosted checks and complete frozen comparison. Accept the baseline only after applicable checks finish successfully; pending, cancelled and skipped checks are not passes.

The subsequent human topology decision selects the shared primary OpenNext Worker, with separate HTTP and Queue delivery modules and one narrow composition entry point; ADR-0019 owns that decision. Preserve the historical separate-Worker preference and proof limitations, including acknowledged-message redelivery after another message throws. The exception permits only the complete bounded dispatch, consumption and terminal-failure candidate. Jobs remains optional on current portfolio/site/app profiles, requires foundation but no implicit persistence, preserves default recipes and retained behavior, and introduces no business jobs or second topology. Reconcile existing persistence/email catalog, deployment, standards, foundation and lock subjects serially; only new or materially changed subjects receive task-linked pending records with empty evidence.

Preserve historical receipts and certification evidence, immutable generated fixtures, parallel worktrees, provider resources and leases. This exception neither closes P4 nor certifies pending dependencies nor authorizes P5E-3 lifecycle/replay/drain, P5E-C certification, merge or deployment. Procedural Gate 2/3 pauses are prospectively waived for this named delivery; exact plans, frozen comparisons, bounded `@ponytail-review`, independent reviews, dispositions and the private final packet remain mandatory. Before later integration, the owning gate must reconcile P4 closure, accepted current main and ancestry, new/changed dependency subjects, affected verification and applicable hosted checks, then obtain separate integration authority. Source rollback, deployed consumers and queued data remain separate recovery domains.

### One-time 2026-09-23 Queue topology proof exception

The user explicitly authorized only P5E-1 test-only implementation, verification, independent review and preparation of one open non-draft pull request before formal P4 closure. The accepted earlier predecessor is `fe16ae41153f89242cdf48c0f899c3720f511557`, with explicit owner acceptance in the tracked acceptance artifact `docs/implementation-evidence/2026-09-13-app-foundation-app-certification-receipt.md` for retained `app-foundation@0.1.0` (not current pending foundation `0.2.0`). Its ancestry to exact latest-main base `3f62d61e869358ec59eb3e3ea898e222b4d0054b` was verified. Work is isolated on branch `queue-consumer-topology` in the new managed worktree of that name; the exact local path, base checks, frozen comparison and prospective execution authority are retained in private `docs/implementation-evidence/2026-09-23-queue-consumer-topology-preparation.md` and its exact-file plan. Required applicable main checks must pass before accepting that base; pending and skipped checks are not passes.

The scope is the smallest synthetic comparison of a consumer in the primary built OpenNext Worker and a separate Worker, using the pinned graph and existing binding harness where adequate. Only proof-owned tests/fixtures, their verification command and direct documentary consumers may change. Product generation, installed descriptors, historical receipts, certification records, immutable generated fixtures, parallel worktrees, provider resources and leases remain protected. P4 remains open; this exception certifies no pending subject, authorizes neither P5E-2 nor P5E-3, and selects no production deployment topology.

The exception ends with the proof pull request ready for human review. It permits the explicitly requested commits, push, non-deploying CI and configured review, with procedural Gate 2/3 pauses prospectively waived for this task; plans, frozen comparisons, review evidence and the private final packet remain mandatory. It grants no merge or provider/deployment authority. Before any later integration or product configuration, obtain the topology decision and separate authority, reconcile with current accepted main and P4 closure at the owning gate, preserve exact historical evidence, identify any materially changed subject requiring renewal, recheck ancestry and rerun affected verification and applicable hosted checks. No existing exception or completed Web3Forms/Resend implementation is reopened.

### One-time 2026-09-14 persistence implementation exception

The following records the original implementation authority. Its integration-order prerequisite is superseded only for already-merged PR #129 by the [certification-only sequencing amendment](#one-time-2026-09-17-persistence-certification-sequencing-amendment); all other resource, evidence and ownership boundaries remain in force.

The user explicitly authorized application-persistence implementation in parallel with P4 certification on 2026-09-14. Its accepted implementation predecessor is `main@fe16ae41153f89242cdf48c0f899c3720f511557`, the merged diagnostic pull request 127, whose exact post-merge Repository quality run `34799203126` completed successfully. This is an implementation base, not P4 phase acceptance.

The isolated `application-persistence` branch and `.worktrees/application-persistence` worktree may implement and verify the bounded persistence descriptor, finite installed catalog selection, generated D1/Drizzle conventions, binding tests, environment and migration controls, generation/addition/removal/re-addition, removal evidence review, CLI adapters, representative fixture, and their direct documentation and verification consumers. Routine delivery approvals continue to apply to this candidate. Its source and test changes remain isolated from the P4 certification worktree and accepted main.

P4 exclusively owns its exact ten certification subjects, evidence, registry transitions and acceptance, protected worktree, shared stateless deployment, resource lease and designated Brave window. This exception permits no edits to that worktree or its evidence, no substitution of persistence candidate bytes for P4 inputs, and no use of its resources. Existing default app output, historical installed views, the seven retained fixtures and app-foundation's Effect/health contracts remain unchanged. Candidate admission records describe only the candidate's new or materially changed subjects; they neither modify P4's input snapshot nor certify a subject.

P4 certification and explicit phase acceptance integrate first. Before persistence integration, reconcile this isolated candidate with that accepted revision without rewriting or discarding existing work, preserve exact P4 receipts as historical evidence, identify every materially changed deployment/standards or other subject, and require task-linked pending renewal records without carrying accepted evidence onto changed behavior. Recheck ancestry, exact subjects, affected generated-project/lifecycle fixtures, final review and applicable hosted checks against the reconciled candidate. If P4 changes a shared contract, reconcile its canonical owner and direct consumers before integration. There is no concurrent merge or implied deployed-persistence certification.

For this increment, the approved binding lane uses the existing Wrangler `createTestHarness()` from a Vitest 5 Node host, with D1 executing in workerd. Deployment owns shared binding-test configuration; persistence owns D1 specifications. Preserve the separate whole-built-Worker lane and prove the exact dependency graph locally before selecting new pins. This is host-driven Workers integration, not execution inside the Workers Vitest pool; it does not downgrade Vitest or prescribe a runner for future capabilities.

Export-and-remove requires a read-only machine assessment of supplied evidence with reasons, provenance, recommendations and explicit uncertainty, followed by human review of required dispositions and the exact fingerprinted removal plan. Machine recommendations provide no execution authority. Integrity failures and changed evidence refuse before writes. Remote provisioning, credential/permission changes, deployment, remote migrations, export/restore/deletion, spending and production actions retain separate concrete approval.

**Stop gate:** Each retained active capability has its own implementation task followed by a separate certification task, evidence, add/upgrade/removal or ejection behavior, recovery plan, review packet, and approval. Deferred contact retains those duties for reactivation under the [amendment](#contact-and-portability-work-deferred).

### One-time 2026-09-17 persistence certification sequencing amendment

The user explicitly approved this certification-only sequencing exception on 2026-09-17. Persistence PR #129 is already merged at `main@991ab80bf071d9671bd9c465c01f2ca83d253234`; its applicable post-merge Repository quality run `35018517734` passed. P4 remains unclosed. This amendment records how to reconcile that state; it does not retroactively claim that the original integration prerequisite was met.

First complete and obtain explicit acceptance of the original ten exact app-foundation certification subjects, including retained default standards `0.5.0` and deployment-cloudflare `0.3.0`, analytics and booking-calendly. Then certification-only work for already-merged `application-persistence@0.1.0`, `standards@0.6.0` and `deployment-cloudflare@0.4.0` may proceed on a verified descendant of the accepted persistence merge before formal P4 closure. This replaces only the original P4-closure prerequisite for that certification successor. It authorizes no further capability implementation and supplies no evidence or certification status by itself.

Refresh exact subjects, ancestry, accepted source and applicable terminal hosted checks before execution. Preserve the original subject receipts without transferring their evidence to changed digests. Persistent resources, the production-named deployment slot, credentials, migrations, synthetic data, export/restore, cleanup and recovery require their separate concrete design and operation approvals. The shared stateless test-deploy Worker remains excluded from persistence.

Formal P4 closure still requires all eleven current admission records to satisfy their complete evidence and independent-review contracts, the unchanged `all-certified` machine check to pass, and explicit human phase acceptance. No subset closure check, historical-evidence substitution or next-product-increment exception is introduced. The [certification coverage owner](../architecture/capability-model.md#certification-coverage) continues to distinguish retained default subjects from current admission subjects.

### One-time 2026-09-19 transactional-email implementation exception

The user explicitly approved isolated P5D implementation on 2026-09-19 while P4 remains open. Its accepted implementation predecessor is `main@fe16ae41153f89242cdf48c0f899c3720f511557`, supported by accepted receipt `2026-09-13-app-foundation-app-certification-receipt.md` under [certification coverage](../architecture/capability-model.md#certification-coverage) and the tracked registry. That revision is an ancestor of the selected base `main@6a09bfc16fc63488910a8bbc54f3d622a5f047e8`. Post-merge Repository quality run `35461945554` passed every applicable job at that base. Admission passed for eleven subjects; `all-certified` closure rejected the five pending analytics, booking-calendly, application-persistence, standards and deployment-cloudflare subjects. Neither that baseline nor the earlier persistence merge establishes P4 acceptance.

The isolated `transactional-email-resend` branch and `.worktrees/transactional-email-resend` worktree may implement, verify and review the bounded email capability and prepare its pull request after the remaining material design choices are resolved. Scope includes its provider-neutral sender, Resend adapter, environment/domain validation, idempotency, normalized results/errors, privacy-safe events, controlled provider contracts, generation and applicable lifecycle, fixtures, direct consumers and operational/removal guidance. Profile/dependency behavior, public contracts, transport, retry/cancellation, compatibility, privacy and removal choices remain subject to explicit user decisions; this sequencing approval does not choose them.

On 2026-09-22 the user approved all four material recommendations: current-profile support with retained foundation, native HTTP transport, the acceptance/idempotency/deadline/cancellation contract, and privacy plus reviewed source removal. [ADR-0017](../adr/0017-transactional-email-resend.md) records those decisions; the [email boundary](../architecture/capability-model.md#transactional-email-boundary) owns implementation and lifecycle limits. Candidate admission marks the new email and broader foundation subjects pending without altering the protected predecessor evidence.

P4 retains exclusive ownership of its exact certification inputs and subjects, acceptance, certification worktrees, evidence, registry transitions, shared deployment, resource lease and designated browser. This stream must not alter those worktrees or evidence, substitute candidate bytes into their inputs, or use their provider resources. Preserve default and historical installed behavior and unaffected certification subjects. Only new or materially changed candidate subjects receive task-linked pending records with empty evidence; no historical receipt certifies changed behavior. Shared contracts must not be changed concurrently without reconciliation with their canonical owner.

P4 certification, its required closure evidence and explicit phase acceptance must integrate before this email candidate may merge. Then reconcile onto accepted main without losing work, preserve exact historical receipts, renew affected subjects as pending, and recheck ancestry, generation/lifecycle fixtures, affected verification, final review and applicable hosted checks. Merge remains sequential and subject to repository protections. If that predecessor remains unavailable, deliver the verified pull request and identify the open gate. This exception does not extend or retroactively satisfy the persistence integration exception.

The user's delivery authorization permits routine in-scope commits, pushes, pull-request work and an eligible merge, successful applicable post-merge checks and clean-main reconciliation. Separate procedural Gate 2 and Gate 3 pauses are prospectively waived for this run after material design choices are resolved; the exact-file plan, frozen comparison, independent reviews and final packet remain required. This records prospective authorization, not personal inspection of an unseen final diff. Live sends, provider/account/DNS changes, credentials, deployment, spending, persistent-data operations and capability certification remain separate work. This exception creates no precedent for another capability.

## P6 — Payload CMS

The [owning delivery cards and certification rows](2026-09-16-remaining-program-delivery-plan.md#application-environment-acceptance) include environment configuration, composed guide/examples, negative cases and recovery at first executable delivery; existing prerequisites and phase closure remain.

Add separate CMS D1/R2, explicit Payload adapters, file import and parity, drafts/preview, authority cutover, localization integration, export, and recovery. P6 follows stable content and multilingual contracts.

**Stop gate:** Staging parity, cutover, export, and recovery evidence is approved.

## P7 — Authenticated app

The [owning delivery cards and certification rows](2026-09-16-remaining-program-delivery-plan.md#application-environment-acceptance) include environment configuration, composed guide/examples, negative cases and recovery at first executable delivery; existing prerequisites and phase closure remain.

After accepted certification of P5C, P5D and the required existing shared subjects, run the separately authorized deployed Better Auth/Next.js/OpenNext/D1/Drizzle compatibility and security spike. Implement individual accounts, verified email/password, Google sign-in, sessions, protected use cases, account profile, export/delete hooks, roles, audit events, abuse controls, and the narrow support console. P7 does not require jobs or durable contact submissions.

### Initial authenticated-composition staging

The user approved this bounded first-delivery sequence on 2026-09-16. The [review protocol](../governance/review-and-contribution.md#initial-authenticated-composition-exception) owns its implementation/certification start-order exception and unchanged approval controls.

The named composition consists only of `identity-core`, `identity-google`, `protected-area`, `account-profile` and `support-console`, plus the shared changes essential to their first complete authenticated-app recipe. The capability model continues to own identifiers, dependencies, support and removal policies. Existing shared capability implementations remain predecessors; this does not restart them.

Deliver the accepted compatibility/security proof, then the complete fresh recipe through focused internal work packages. Separately certify changed shared prerequisite subjects first, identity-core next, protected-area/identity-google/account-profile after their declared dependencies, and support-console after its declared dependencies. Conclude with the complete fresh-baseline checkpoint. Shared generated scenarios may supply causal assertions for several subjects, but there is no composite certificate.

Only after that checkpoint, deliver the exact supported app-to-authenticated-app migration and applicable capability lifecycle, followed by separate renewal of every materially changed subject. P7 closes at its existing stop gate. TOTP, passkeys, payments and broader product scope remain later work.

**Stop gate:** Security, privacy, migration, authorization, deployment, and recovery evidence is approved.

## P8 — Independent identity assurance and payments

The [owning delivery cards and certification rows](2026-09-16-remaining-program-delivery-plan.md#application-environment-acceptance) include environment configuration, composed guide/examples, negative cases and recovery at first executable delivery; existing prerequisites and phase closure remain.

- P8A: independently migratable TOTP and recovery codes.
- P8B: independently migratable passkeys and recovery.
- P8C: independent Stripe payments with `one-time`, `subscriptions`, or `both`.

These tracks may proceed separately after their prerequisites stabilize. One-time payments may resolve app-foundation, persistence, and jobs; subscription validation uses a stable `BillingSubjectProvider` supplied initially by authenticated-app.

**Stop gate:** Each selected track completes its implementation task and separate certification task, then passes cross-capability, security, persistent-data/provider, reconciliation, and recovery review.

## P9 — Booking webhooks

The [owning delivery cards and certification rows](2026-09-16-remaining-program-delivery-plan.md#application-environment-acceptance) include environment configuration, composed guide/examples, negative cases and recovery at first executable delivery; existing prerequisites and phase closure remain.

Implement verified/idempotent Calendly events, normalized booking events, optional queue processing, replay, diagnostics, and failure recovery without creating a CRM.

**Stop gate:** Security, idempotency, replay, and recovery evidence is approved.

## P10 — Fleet hardening

The [owning delivery cards and certification rows](2026-09-16-remaining-program-delivery-plan.md#application-environment-acceptance) include environment configuration, composed guide/examples, negative cases and recovery at first executable delivery; existing prerequisites and phase closure remain.

After representative synthetic client-journey evidence, reassess package boundaries, measure inference and migration failures, simplify weak abstractions, verify supported upgrades and run data/provider recovery drills. Active order is P10-1 → evidence-selected P10-2 repairs/renewals → P10-3 → P10-5 → final acceptance of the amended active scope. P10-4 and its contingent package/support decisions are [deferred](#contact-and-portability-work-deferred); no portability evidence or second supported production platform is claimed.

At the end of the final fleet-hardening task, run permanent documentation hardening: inventory architecture, ADR, governance, and agent-instruction sequencing labels; replace implementation-routing references where provenance is no longer necessary; and narrow the scanner exemptions while preserving historical records and content whose subject is genuinely sequencing.

**Stop gate:** Present all retained active obligations, fleet evidence, applicable certification closure, residual risks and the two explicit deferrals for final user acceptance of the amended scope. Retained support or evidence-led extraction decisions still require approval; deferred contact and P10-4 evidence do not block that scoped acceptance and are not claimed complete.

### Delivery authorization extension — 2026-09-23

The user prospectively approved necessary future amendments within this increment, requested waiting for and addressing CodeRabbit findings after opening the MR, and authorized merging the completed MR using the sole-developer bypass after required verification and review. They requested a separate new task to begin immediate Web3Forms certification after merge, carrying appropriate resource-creation/change authorization. This extends the earlier open-MR endpoint; it does not claim review of an unseen final diff or remove substantive quality, privacy, or certification gates. Historical approvals and global governance remain intact. Material decisions beyond the approved scope still require concrete resolution.

### Contact implementation CI capacity amendment — 2026-09-23

Under the user’s prospective amendment authorization, allow the non-deploying `builder-and-packages` quality job up to 60 minutes for the complete expanded compiled lifecycle suite. The accepted baseline run took 41m45s; the two new controlled lifecycle cases took a further 6m12s locally, making the previous limit of 45 minutes an evidenced capacity risk. This is a bounded runtime allowance, not a recorded hosted timeout or a reduction in verification. Preserve every check, security gate, permission and other job limit. The workflow owns the exact timeout and the constitution check retains its finite bound. Independent review and successful latest-revision hosted checks remain required before the authorized merge.
