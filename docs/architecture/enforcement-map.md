# Architecture Enforcement Map

This document is the canonical owner for mapping invariants to actual or planned gates. “Planned” means the gate does not exist yet; documentation must not describe it as passing. Phase ownership follows the [program roadmap](../roadmaps/program-roadmap.md).

| Invariant | Rule | Gate status | Automated owner | Phase |
|---|---|---|---|---|
| `INV-PROFILE-MATERIALIZATION` | Resolved installed capabilities become authoritative; no live profile inheritance | actual for deterministic resolution; explicit independent Calendly, multilingual, and analytics selection; `portfolio@0.10.0` and `site@0.11.0` materialization; installed-manifest projection; readable retained recipe provenance; read-only state/capability evidence; read-only diagnostics across desired, installed, and inferred capabilities; desired-project/render agreement; new-directory manifest/pre-state/post-state agreement; exact CLI inspection; byte-stable committed fixtures; and the exact `site-routing@0.3.0` to `0.4.0` reconciliation edge | `builder-core` contract, resolver, manifest, inference, diagnostics, rendering, generation, and lifecycle tests; CLI tests; `tests/generated-fixtures/determinism.test.mjs`; `verify:generated-skeletons`; `verify:generated-visuals` | P1/P2/P3B |
| `INV-CAPABILITY-METADATA` | Every capability declares normalized delivery, state, removal, security, ownership, inference, migration, and verification metadata | actual for the nine executable descriptors, including strict Calendly, multilingual, and analytics settings/selection agreement; analytics provider/domain/Content Security Policy/storage/cookie/purpose/data/retention/removal declarations; deployment and observability security metadata; pure ownership materialization; drift evidence; content-safe diagnostics; rendered ownership; and committed fixture state/inference agreement; remaining program catalog enforcement planned | `builder-core` schema, catalog, contract, resolution, state-ownership, inference, diagnostics, rendering and generation tests; CLI and generated-fixture tests | P1/P2/P3B |
| `INV-CAPABILITY-CERTIFICATION` | Every new or materially changed executable capability receives an independently planned certification task after implementation, with current human prerequisites and causal local plus conditional external evidence | actual tracked certification coverage registry at [`certifications/capabilities.json`](../../certifications/capabilities.json), strict schema, clean-checkout descriptor admission and closure, plus two separately invoked pre-registry-update private gates: one for local plan/receipt presence, subject/revision/outcome binding, ancestry, and completed review, and one for content-safe tracked receipts with exact ignore exceptions; exact `standards@0.4.0` is certified from the accepted local `existing-repository-lifecycle` and renewed `fresh-scaffold` evidence at evidence revision `d7f9dac6e25d5dde32015968d0912b45e73644e7`, and prior `standards@0.3.0` evidence remains historical for that exact subject only; the private `observability@0.2.0` receipt remains historical evidence only for that exact historical subject; active `booking-calendly@0.1.0` is certified from current-digest fresh-add lifecycle evidence at revision `b30e10b86b9ac9ef8dfdf1e8fa8e4077e2abe059` and protected-staging, provider-confirmed, cancellation, and cleanup evidence at revision `f9bd78f115c2118afd6dcc17ce49b2bfe34ca10d`, while every prior-digest initial-scaffolding/provider receipt remains historical; `observability@0.3.0` and `deployment-cloudflare@0.3.0` remain certified from their accepted exact-subject evidence; exact `content-files@0.4.0` is certified from accepted local `fresh-scaffold` evidence at revision `f03b9f624c370728f678924ce34e5287558d2a87`; exact `section-composition@0.3.0` is certified from accepted local `fresh-scaffold` evidence at revision `f74459c8833833186bb651c116ed524e51044677`; historical exact `site-routing@0.3.0` is certified from accepted local `fresh-scaffold` evidence at revision `77cea944513e521939bf4de088048f67acdfbc3c`; current exact `site-routing@0.4.0` is certified from accepted local `fresh-scaffold` and `existing-repository-lifecycle` evidence at revision `6034d7330af912d1a1b9bcff3323ed360ebee2d0`; exact `multilingual@0.1.0` is certified from accepted local `fresh-scaffold` and `existing-repository-lifecycle` evidence at revision `96b587a254cf6fc859867d6fc66c7e0c900c4cfd`; exact `analytics@0.1.0` is certified from accepted `cleanup-recovery`, `deployed-application`, `existing-repository-lifecycle`, `fresh-scaffold`, and `provider-confirmed` evidence at revision `a97341ea628210b6fa713fb12461084f20c3f8da`; descriptor admission and `all-certified` closure pass; a descriptor version or behavior-contract digest material change requires a new task-linked pending record | builder-core certification contracts/tests; `tests/capability-certification`; clean-checkout `check:capability-certification`; local-only `check:private-capability-certification`; `tests/constitution/private-workflow-artifacts.test.mjs`; capability-specific runners and externally authorized receipts | P2 onward |
| `INV-CLOUDFLARE-ISOLATION` | Cloudflare types and bindings remain in adapters, configuration, integration tests, and composition roots | actual for the P0.2 proof, dual-major standards behavior, public installed generated-repository lint/typecheck/Next/OpenNext builds, all three isolated committed-fixture builds, and local OpenNext/workerd browser certification; Calendly integration templates contain no Cloudflare imports or types; observability's sole Cloudflare import is in its explicit adapter; Wrangler type generation succeeds; live deployment remains separate | `@egeria-systems/standards/eslint/cloudflare-isolation`, `proofs/nextjs-cloudflare/eslint.config.mjs`, proof gates, builder rendering/generation tests, committed fixtures, and `scripts/verify-generated-skeletons.mjs` | P0.2/P0.3/P1/P2 |
| `INV-NARROW-PORTS` | Ports are narrow and owned by consuming boundaries | planned | architecture tests plus bounded review | P4 onward |
| `INV-NO-GENERIC-PLATFORM-PORT` | No generic `PlatformService` or `ApplicationDatabase` port | planned | architecture tests plus bounded review | P4 onward |
| `INV-PACKAGE-EXTRACTION` | Public extraction requires consumers/boundary, stable API, lifecycle value, tests, ownership, migration, and cost evidence | actual for P0.3 ownership shells and APIs; later extraction reassessment planned | package-boundary manifest, API, consumer, dependency-direction, and no-premature-surface tests | P0.2/P0.3/P10 |
| `INV-PACKAGE-PUBLICATION` | Only approved public packages expose allowlisted files and APIs; local release configuration never authorizes publication | actual for exact `0.1.0` bootstrap, both `0.2.0` OIDC/provenance releases, and observability `0.3.0`, exact history/target registry gates, manifest/API/tarball allowlists, manual exact-`main`-commit dispatch, and unconditional auth cleanup; every future publication remains separately approval-gated | manifest, API, tarball, release-context, registry-history, signature, and provenance tests/evidence; manual `package-release.yml` workflow | P0.3/P1/P2 |
| `INV-SEMANTIC-NAMING` | Roadmap and implementation-sequencing labels remain provenance rather than software responsibility | actual for live tracked and non-ignored untracked paths plus authored executable, configuration, workflow, test, template, fixture, generated, and user-facing Markdown text; internal and provenance documents remain content-exempt until permanent documentation hardening at the end of the final program task | `scripts/check-semantic-naming.mjs` and `tests/constitution/semantic-naming.test.mjs` | repository-wide |
| `INV-CLEAN-ISOLATED-MIGRATION` | Builder transformations require clean state and one isolated-worktree execution | actual and accepted for the exact `booking-calendly@0.1.0` `plan-add`, `apply-add`, `plan-remove`, and `apply-remove` boundaries, exact standards and site-routing `plan-upgrade`/`apply-upgrade`, and exact `portfolio@0.10.0` to historical `site@0.10.0` `plan-profile-transition`/`apply-profile-transition`; routing evidence proves clean stable attached linked-worktree identity, exact plan fingerprint, absent non-ignored create targets, exact expected dirty paths, failure-prefix retention, and final byte rereads; `plan-remove` remains ownership-aware, selecting delete for unchanged capability-owned files and preserve/eject for modified files, while every planner redacts private inputs and performs no write; private shared control persistence changes no precondition; worktree/branch creation, a generic lifecycle executor, any further upgrade/profile edge, and every other removal executor remain planned | builder-core Git-inspection, capability-addition, capability-removal, capability-upgrade, and profile-transition planner/executor tests plus operation-specific writer, shared control-persistence, transaction, failure/recovery, fingerprint, exact-control, no-mutation, and exact-diff tests; CLI temporary linked-worktree integration tests | P3/P3B |
| `INV-SUPPORTED-UPGRADE-EDGE` | Only the exact declared capability edges `standards@0.3.0` to `standards@0.4.0` and `site-routing@0.3.0` to `site-routing@0.4.0` may produce a read-only fingerprinted plan or consume it through exact `apply-upgrade` | actual and accepted for both exact edges: exact historical/current catalog and recipe snapshots, endpoint-subject digests, source-version inference, desired/installed/inferred/project/state/migration agreement, exact surface inventory, operation-owned target actions, exact control-file and target fingerprints, clean stable attached isolated-worktree enforcement, privacy-safe output, already-current/missing-edge/ambiguous/incompatible/drift/collision refusal, byte-exact planning no mutation, exact execution/persistence order, failure paths that retain an inspectable prefix and never trigger automatic rollback, and a separate verified-final-diff stop; current site-routing lifecycle certification is bound to exact evidence revision `6034d7330af912d1a1b9bcff3323ed360ebee2d0`, while the public target endpoint retains accepted ancestor `e69c28ec4228622fd34517a72858e2ac55401a5a` as provenance; the approved source plan owns the contract, and implementation does not deploy, publish, or establish production readiness | `plan-capability-upgrade.test.mjs`, `capability-upgrade-writer.test.mjs`, and `apply-capability-upgrade.test.mjs`; CLI parser, injected-runner, and compiled temporary linked-worktree success/refusal/no-mutation tests; finite named examples are used because the two-edge matrix is not materially combinatorial | P3/P3B |
| `INV-SUPPORTED-PROFILE-TRANSITION` | Only exact `portfolio@0.10.0` to `site@0.10.0` may produce a read-only profile-transition plan or consume it through exact `apply-profile-transition` | actual and accepted through pull request 50 for exact planning and fingerprint-gated execution: builder-core-owned source inference and desired/installed/inferred/project/state/migration/recipe/capability/surface agreement, seven render-derived actions, complete private fingerprint material, clean stable attached isolated-worktree and ignored-create-target enforcement, privacy-safe one-line output, exact execution/persistence order, operation-specific writer, migration `transition-portfolio-0-10-0-to-site-0-10-0`, `profile-transition` receipt, exact nine-path diff, conservative recovery, and separate verified-final-diff stop; the local portfolio-to-site transition lifecycle certification is complete at exact evidence revision `8098c68c82aaa35a59345706c851e8111d463111`; private shared control persistence and certification do not generalize the edge; another edge, a generic lifecycle executor, deployment, provider action, publication, and production authority remain separate | `plan-profile-transition.test.mjs`, `profile-transition-writer.test.mjs`, `apply-profile-transition.test.mjs`, and shared control-persistence tests; CLI parser, injected-runner, compiled temporary linked-worktree success/refusal/privacy/repeatability/no-mutation/transaction tests, and `certify-profile-transition-lifecycle.mjs` exact-name/TAP/revision/status/index-flag guards; finite named examples only | P3 |
| `INV-REMOVAL-REFERENCE-GUARDS` | Automated removal analysis classifies exact resolved and descriptor-specific conflicts separately from heuristic and incomplete-coverage warnings; finding no detected match may never be represented as proof of dependency absence or complete removal | planned; accepted removal plans currently require fingerprint-bound repository-wide manual review and exact preserved/ejected-path reconciliation but do not enumerate arbitrary repository source | planned builder-core inventory, capability-specific analyzer, removal planner/executor, CLI, temporary-repository, and no-mutation tests | P3C |
| `INV-STATE-UPDATE-ORDER` | State records update after transformation, verification, and post-change inference | actual for new-directory generation; exact Calendly, multilingual, and analytics addition/removal; standards and site-routing upgrades; and portfolio-to-historical-site transactions through state-last failure injection, VCS-metadata-free isolated ordinary verification, pending inference, conservative persisted receipts, migration-before-state persistence, final manifest/state/inference agreement, exact dirty-path inspection, a final exact changed-byte reread, bounded recovery classification, and the separate verified-final-diff stop; private shared control persistence retains exact order and sources while a generic lifecycle executor, recovery automation, and verified-final-diff approval automation remain planned | `builder-core` `generate-project.test.mjs`, operation-specific executor tests, shared control-persistence tests, and CLI compiled transaction tests | P1/P3/P3B |
| `INV-COPY-EXTERNALIZATION` | User-visible/translatable copy comes from validated content/localization | actual for the P0.2 proof, strict generated YAML 1.2 configuration/content including skip-navigation and booking copy plus Markdown front-matter/body placement, all three committed fixtures, isolated generated Next/OpenNext builds, and static JSX, relevant attribute, and Next.js metadata literal checks across canonical builder TSX templates including integration templates under both supported ESLint majors; generated-repository locale-key validation and parity, runtime copy resolution beyond the current paths, Markdown rendering safety, translation quality, and semantic quality remain separate | proof parser/pure-render unit test, builder content-parser/rendering/generation tests, generated-fixture determinism and build harness, standards dual-major copy-rule tests, root template lint config, and `check:copy-externalization`; later generated-project and locale gates | P0.2/P1/P2 |
| `INV-MULTILINGUAL-ROUTING` | Exact `en-CA` and `fr-CA` locale-prefixed public routes include the default locale, unprefixed requests negotiate once, and explicit unsupported locales receive not-found behavior with no fallback | actual for the constitution contract requiring locale-prefixed public paths and no fallback; runtime enforcement is required in the candidate through generated locale/proxy unit tests, metadata and sitemap tests, development and OpenNext/workerd browser journeys, retained-fixture verification, and exact add/remove composition tests before implementation acceptance | constitution contract now; candidate builder render/generation/lifecycle tests, generated Vitest and Playwright tests, compiled CLI journeys, fixture determinism, and fixed-root verifier | P3B |
| `INV-MULTILINGUAL-CONTENT` | Locale catalogs reject missing and unused keys, require route/page/section structural parity, and supply all translatable runtime copy without cross-locale fallback | actual for the constitution contract requiring missing-key rejection, unused-key rejection, and structural parity; runtime enforcement is required in the candidate through strict generated catalog parsing, parity mutation tests, build checks, copy externalization, and exact lifecycle composition before implementation acceptance; linguistic and semantic quality remain human-review concerns | constitution contract now; candidate builder rendering tests, generated Vitest tests, copy externalization, compiled lifecycle journeys, fixture determinism, and fixed-root verifier | P3B |
| `INV-BOUNDED-SECTIONS` | Content configures only source-registered typed sections and cannot inject executable component behavior | actual for strict generated YAML section unions, four exact registry entries, variants/profile/accessibility/analytics/migration metadata, pure semantic responsive components, semantic global design tokens, exact Tailwind CSS/PostCSS and source ownership, deterministic portfolio/site fixtures, and isolated generated builds; visual assessment, browser behavior, later analytics hooks, schema migrations, and CMS authority remain separate | builder parser/registry/rendering/resolution/generation tests, token-contrast source tests, root copy lint, package-boundary inventory tests, fixture determinism/inference, and generated lint/typecheck/Next/OpenNext verification | P2 |
| `INV-GENERATED-TESTING` | Generated unit, component, and browser checks use explicit runtime boundaries and semantic non-watch CI commands | actual for named Vitest Node/jsdom projects, explicit imports, deterministic component cleanup, real parser and presentation starter specifications, exact package/script/config/setup ownership, native TypeScript-path resolution, generated quality workflow steps, all four retained fixtures, and one automatic repository workflow with stable `scope`, `builder-and-packages`, `generated-projects`, `compatibility-proof`, and pull-request-only `dependency-review` jobs; validated job-level scoping fails safe, the exact repository-quality workflow activates both deep lanes while unrelated manual-workflow-only changes do not, reusable pnpm caches are disabled, generated browser jobs use the official Playwright image matching the installed version at an immutable digest with a bounded 1 GiB shared-memory allocation and install only Chromium, and generated/proof verification builds standalone Next output once before the OpenNext `--skipNextBuild` transform and direct prepared-output preview; the predecessor three-workflow exact candidate `93e4e9f6ea944329de7c47c9e8bf34382774b1f8` and final generated `main@47eb9ef09ea412dbfcf14f69dda153fb14a0691b` passed hosted quality runs, with final run `32315057638` proving the current generated workflow bytes; jsdom does not claim layout/browser behavior | builder rendering/generation/verifier tests; generated fixture determinism and verification tests; `verify:generated-skeletons`; constitution workflow/instruction contracts including actual-shell scope scenarios; generated `test:unit`, `test:component`, and Playwright commands; hosted quality run `32315057638` and historical runs `31583624246`, `31583624223`, and `31583624387` | P2 |
| `INV-VISUAL-REGRESSION` | Deterministic visual regression covers representative generated responsive states without a combinatorial matrix | actual for byte-exact binary PNG generation, managed visual configuration, application-owned specification and baselines, exact portfolio/site 1440-by-900 and 320-by-800 comparisons against prepared OpenNext/workerd output, the flag-free generated `test:visual` command, and root `verify:generated-visuals`; CI uses the digest-pinned Linux/Chromium image, never updates snapshots, stages baseline-update inputs through a read-only index snapshot with only two named PNG outputs, preserves earlier browser diagnostics by sequencing visual comparison after preview success, exports root-verifier artifacts before isolated cleanup, and retains failure-only expected/actual/diff diagnostics for seven days; performance budgets are deferred by the unnumbered P2 closure amendment and no performance claim is made because no performance gate is present; screenshot equality makes no visual-quality, human-accessibility, deployed, production, or WCAG-conformance claim | builder rendering, binary-template, ownership, certification, workflow, fixture, and verifier artifact-export tests; generated `test:visual`; `verify:generated-visuals`; generated and repository quality workflows | P2 |
| `INV-ACCESSIBILITY-AUTOMATION` | Generated applications run automated WCAG 2.2 AA-relevant checks | actual for P0.2 proof; source-level skip-navigation, landmark/focus, colour-token contrast, link-target, wrapping, responsive-grid, and reduced-motion contracts are actual for generated portfolio/site output; all three current retained fixtures pass local development and workerd Playwright/axe checks, including Calendly ordinary-link fallback, native-dialog focus/close behavior, 320-pixel containment, and selected axe rules against a stubbed provider document; deployed execution, provider behavior, human evaluation, and any conformance claim remain separate | builder content/presentation/style and browser-contract tests; fixture determinism/inference; `scripts/verify-generated-skeletons.mjs`; generated development/preview Playwright suites | P0.2/P2 |
| `INV-ACCESSIBILITY-CLAIMS` | Automation alone never supports a conformance claim | actual for P0.2 documentation and current generated source/build claim limits; generated release check and any human evaluation remain planned | constitution compatibility-record contract, current implementation evidence, and bounded review | P0.2/P2 |
| `INV-ANALYTICS-SEPARATION` | Cloudflare Web Analytics and other analytics never arrive through observability | actual for exact `analytics@0.1.0`: independent selection, no observability descriptor dependency, no observability source import or event bridge, byte-stable unselected generation, and negative catalog/template/rendered/fixture checks | builder catalog, resolution, rendering, generation, lifecycle, source-boundary, and fixture tests | P2/P3B |
| `INV-ANALYTICS-CONSENT` | A versioned local preference uses purpose as the canonical visitor choice key, exactly matches the current notice and provider context, expires after 180 days, fails closed, synchronizes persisted changes, and reloads after successful reduction without making a compliance claim | actual for the version-2 consent record; exact notice, provider-context, configured-purpose, and 180-day validation; per-purpose transitions; denial-before-load ordering; persistence-verified reductions; honest incomplete-revocation handling; localized first-layer and management actions; same-origin storage-event synchronization; exact add/remove/re-add lifecycle; locally intercepted development/workerd browser behavior; and accepted exact-subject deployed/provider certification evidence at revision `a97341ea628210b6fa713fb12461084f20c3f8da`; the evidence remains bounded to its recorded synthetic journey and does not establish legal compliance, privacy completeness, production readiness, or ongoing provider availability | builder contract, rendering, and lifecycle tests; generated Vitest unit and component specifications; `site-multilingual-analytics` Playwright specification; fixture determinism; `verify:generated-skeletons`; and the tracked analytics certification receipt | P3B |
| `INV-ANALYTICS-PROVIDER-BOUNDARY` | Cloudflare Web Analytics, Google Analytics 4, and Microsoft Clarity retain unique fixed purposes and runtime loaders; Search Console and Looker Studio remain operational integrations; domains, Content Security Policy, storage, cookies, data, retention, provider state, and removal are explicit | actual for descriptor metadata, strict provider settings and dependencies, generated provider declarations and operator guidance, redacted plans, selected-provider inference, exact add/remove/re-add lifecycle, separate provider-state removal review, and no-provider-mutation tests | builder contracts/catalog/rendering/generation/addition/removal/CLI tests; generated provider-contract unit tests; fixture/state/inference checks | P3B |
| `INV-DEPLOYMENT-AUTHORITY` | GitHub Actions is the sole deployment authority, and ordinary quality workflows have no deployment or write authority | actual for the P0.2 workflow and deployed non-production proof; actual structurally for the consolidated read-only, no-secret repository workflow, generated read-only quality workflow, and generated separate manual workflow with exact-main revision guard, fixed `production` environment, least privilege, pre-credential verification, digest-pinned version-matched Playwright job runtime with a bounded 1 GiB shared-memory allocation, deploy-only secret step, and post-deploy HTTPS browser smoke; final generated `main@47eb9ef09ea412dbfcf14f69dda153fb14a0691b` passed exact deployment run `32315740366`, and direct environment/provider readback completed the accepted deployment and retained-baseline recovery outcomes for certified `deployment-cloudflare@0.3.0` | constitution workflow-policy contract; builder rendering, real-shell guard, ownership, inference, generation and fixture tests; manual `compatibility-proof.yml`; generated `quality.yml` and `deploy.yml`; consolidated `repository-quality.yml`; recorded P0.2 deployment run; generated deployment run `32315740366`; historical quality runs `31583624246`, `31583624223`, and `31583624387` | P0.2/P2 |
| `INV-P0-1-NO-PREMATURE-RUNTIME` | P0.1 contains constitution files only, with no app/package/state/workflow/provider surface | actual | final-tree inspection and review packet | P0.1 |

The accepted P0.2 evidence snapshot, command meanings, runtime distinctions, deployment evidence, and claim limits are recorded in the [Next.js and Cloudflare compatibility record](../compatibility/nextjs-cloudflare.md); executable manifests own current proof dependency versions. Local workflow structure proves only the repository contract; the recorded successful GitHub Actions run and non-production Worker evidence establish the separate external deployment result.

The deterministic in-memory rendering of YAML 1.2 content and Markdown with validated YAML front matter is guarded by `packages/builder-core/tests/render-skeleton.test.mjs` and the exact source and template allowlists in `tests/package-boundaries/private-packages.test.mjs`. These gates prove returned bytes, strict content shapes, catalog/file boundaries, copy placement, and ownership agreement only. The Markdown body remains opaque data; these gates do not prove safe HTML rendering.

P1 new-directory generation is guarded by `packages/builder-core/tests/generate-project.test.mjs`. Its injected-verifier tests prove exclusive source writes, exact lockfile inventory, pre-state and post-state agreement, state-last ordering, exact installed surfaces, destination refusal, identity-bounded cleanup, and stable failure behavior. The explicit `packages/builder-core/tests/generate-project.integration.mjs` gate separately proves that production generation resolves the exact public standards `0.1.0` and observability `0.3.0` packages with their recorded integrity and signature/provenance status, completes frozen installation plus Next/OpenNext builds, the generated graphs pass the execution-time moderate advisory and registry-signature audits, and final state/inference agree. Neither generation nor these checks prove workerd runtime, deployment, provider delivery, visual/performance quality, accessibility conformance, translation quality, human usability, privacy completeness, or production safety.

The committed portfolio, `portfolio-calendly`, site, `site-multilingual`, and `site-multilingual-analytics` golden fixtures are guarded by LF-normalized byte-for-byte regeneration, exact inventory/state/inference checks, and `verify:generated-skeletons`. The fixed-root verifier rejects non-regular paths and unapproved dependency sources or execution policy, validates the exact manifests and workspace policy including the narrow exact first-party release-age exclusion, sole security override, and lifecycle-build allowlist, checks exact public Egeria versions/integrities and lockfile policy, and runs frozen install, peers, advisory/signature audits, lint, Wrangler type generation, typecheck, named unit/component tests, one standalone Next build, the OpenNext `--skipNextBuild` transform, isolated Chromium installation, and development/direct-workerd-preview browser tests only in identity-bounded temporary copies. The analytics fixture locally intercepts every selected provider origin and proves only generated behavior. Visual comparison remains limited to the three existing visual-enabled fixtures and their four profile screenshots; neither multilingual fixture adds a visual baseline or comparison.

Observability rendering and inference tests require all five error UI/content surfaces, Workers Logs custom logging with default request/response invocation logs disabled, separately classified platform error/exception logs, and the browser-route test drives a no-`Content-Length` stream past 8,192 bytes and requires immediate cancellation. Generated-reporter tests execute credential-free and referrer-free browser error delivery, safe operational events to Workers custom logs, restricted diagnostics only through the Better Stack diagnostic adapter, and valid/absent provider composition. Package and generated contracts keep provider portability at the adapter/composition boundary. Descriptor tests keep repository/deployment configuration removal, credential/provider/data disposition, and recovery as separate requirements. Message/stack sanitization is asserted as bounded exposure reduction, not a privacy guarantee, and the increment has no source-map deobfuscation. Certification tests bind each exact subject to its declared evidence contract; exact `observability@0.3.0` is certified only from its own complete three-outcome evidence and cannot inherit the prior subject's evidence.

The observability fresh-scaffold runner passed at evidence revision `ef845b1e0551d3b43e17969cc00f21960c90769b` and remains historical local evidence for the prior subject. The private protected-staging receipt at `ee1e1df10fa2be2f09333efecd86de7f7a131d49` records one deployed journey and bounded Better Stack and Workers Logs review for the exact historical `observability@0.2.0` subject only. Exact `observability@0.3.0` is certified from the accepted fresh-scaffold, deployed-application, and cleanup-recovery receipt at revision `bdcc55f1bfa6eca392ce3e36bdc35adb6f085bad`, diagnostics run `31925083913`, and recovery run `31925927776`. That bounded certification does not prove durable delivery, ongoing provider availability, production readiness, privacy completeness, visual quality, human accessibility, or WCAG conformance. `waitUntil()` remains best-effort and non-durable, and platform/framework logs remain separate provider-controlled records.

The Calendly browser specification intercepts the complete configured provider origin, fulfills only the exact scheduling document with local stub content, aborts and records every unexpected provider-origin request, and asserts that none occurred. It verifies no eager request, popup activation and cleanup, ordinary navigation fallbacks, narrow containment, and selected automated accessibility rules. These recurring gates do not call Calendly, make or confirm a booking, configure or clean provider data, deploy, call the generated HTTPS deployed-mode target, publish, assess visuals, translation quality, human usability, or assistive-technology compatibility, establish accessibility conformance, or prove general security or production safety. A separate one-time provider receipt records the exact-revision protected-staging, provider-confirmed, cancellation, and cleanup evidence; it does not change those automated claim limits or establish ongoing provider availability.

This documentation contract by itself does not prove any runtime or provider result, cleanup, recovery, deployment, or external authorization. The actual registry, gate commands, local receipt, and provider receipt are the distinct evidence owners; this document only names their enforcement and claim boundaries.

## Updating enforcement ownership

When a planned gate is implemented, update this table in the same focused change as the executable gate and its test. Record the exact command and result in implementation evidence. Passing a document-structure contract proves only structure and ownership; it does not prove prose semantics or a later runtime gate.
