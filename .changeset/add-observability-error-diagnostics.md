---
"@egeria-systems/observability": minor
---

Add schema `2.0.0` safe operational events and a distinct restricted diagnostic contract with guarded error capture, redaction, deterministic bounds, separate dispatch, Better Stack encoding, and 8,192-byte browser error envelopes. This pre-1.0 minor is not backward compatible with the published schema `1.0.0` contract and adds zero runtime dependencies.
